# Activity Summary for 4/27/2026

## 1:06:17 AM
The provided log details changes to a single file: `c:\NTHU\Junior_I\Data Lab\Cliffhanger_app\cliffhanger_app\src\services\api\client.ts`.

**File-specific updates for `c:\NTHU\Junior_I\Data Lab\Cliffhanger_app\cliffhanger_app\src\services\api\client.ts`:**

*   **Initial state (4/26/2026, 6:31:48 PM):** The `DEV_HOST` constant, used for constructing the `BASE_URL` for non-web platforms, was set to `'10.1.160.207'`. The file otherwise defines a generic HTTP client with `request` function handling `GET` and `POST` methods, content type, and Bearer token authentication. It also includes logic for differentiating `BASE_URL` based on `Platform.OS` (web vs. physical/emulator).
*   **Update (4/26/2026, 9:38:19 PM):** The `DEV_HOST` constant was updated from `'10.1.160.207'` to `'172.20.10.2'`. All other aspects of the file, including the `request` function and `apiClient` methods, remained unchanged.

**Timestamps of significant changes:**
*   **4/26/2026, 6:31:48 PM:** Initial configuration of the `DEV_HOST` to `10.1.160.207`.
*   **4/26/2026, 9:38:19 PM:** Update of the `DEV_HOST` to `172.20.10.2`.

**Patterns or recurring elements:**
The primary pattern observed is the repeated modification of the `DEV_HOST` IP address within the `client.ts` file, suggesting active development or testing of backend connectivity where the host IP might frequently change. The core logic for the API client, including request handling, error management, and authentication headers, remained stable across these changes.

## 3:09:25 AM
The log primarily details changes to the `c:\NTHU\Junior_I\Data Lab\Cliffhanger_app\cliffhanger_app\src\services\api\client.ts` file, which is responsible for configuring an HTTP client for the application.

**File-Specific Updates:**

*   **`c:\NTHU\Junior_I\Data Lab\Cliffhanger_app\cliffhanger_app\src\services\api\client.ts`**
    *   **Initial Configuration (4/26/2026, 6:31:48 PM):** This file was first configured to establish the HTTP client. It defined a `BASE_URL` that adapts based on the platform (`web` versus `react-native`), utilizing `DEV_HOST = '10.1.160.207'` for non-web environments. It includes a generic `request` function to handle API calls, automatically setting `Content-Type: application/json` and `Authorization` headers (if a token is provided). It also handles error responses by throwing an `Error` for non-OK HTTP statuses. The `apiClient` object exports `get` and `post` methods that leverage this `request` function.
    *   **Host IP Update (4/26/2026, 9:38:19 PM):** The only modification in this update was to the `DEV_HOST` constant. Its value was changed from `'10.1.160.207'` to `'172.20.10.2'`. The rest of the HTTP client configuration, including the `BASE_URL` logic, the `request` function, and the `apiClient` methods, remained identical.

**Timestamps of Significant Changes:**

*   **4/26/2026, 6:31:48 PM:** Initial setup of the core API client, defining the structure for making requests and handling responses.
*   **4/26/2026, 9:38:19 PM:** An update to the development host IP address, indicating a change in the backend server's location or network configuration.

**Patterns or Recurring Elements:**

*   **Consistent API Client Structure:** Both versions of the file maintain a stable structure for the API client, using a `request` function as a core utility and exporting `get` and `post` methods.
*   **Platform-Specific Host Handling:** The use of `Platform.OS === 'web'` to conditionally set the `BASE_URL` (localhost for web, `DEV_HOST` for others) is a recurring pattern, indicating support for multiple deployment environments.
*   **Authorization and Content-Type Headers:** The automatic inclusion of `Content-Type: application/json` and `Bearer` token for authorization is consistently applied.
*   **Error Handling:** The approach to throwing an `Error` when `res.ok` is false, including the HTTP status and body text, is also consistent.
*   **Comments:** Descriptive comments at the top of the file explain its purpose, and inline comments explain the `DEV_HOST` logic for different environments.

## 6:41:40 AM
The provided log details changes to a single file: `c:\NTHU\Junior_I\Data Lab\Cliffhanger_app\cliffhanger_app\src\services\api\client.ts`.

This file defines a core HTTP client responsible for making API requests. It includes logic to configure the `BASE_URL` differently for web, Android emulator, and physical devices, handles `Authorization` headers with bearer tokens, sets `Content-Type` to `application/json`, and provides error handling for non-successful HTTP responses. The `apiClient` object exposes `get` and `post` methods for convenience.

The only significant change observed is an update to the `DEV_HOST` constant within this file:
*   At **4/26/2026, 6:31:48 PM**, `DEV_HOST` was configured as `'10.1.160.207'`.
*   Later, at **4/26/2026, 9:38:19 PM**, `DEV_HOST` was updated to `'172.20.10.2'`.

This indicates a change in the development server's IP address used for non-web platforms (physical devices or Android emulator) during API calls. The rest of the API client's functionality, including the `request` function and the `apiClient` methods, remained consistent across both timestamps.

## 10:42:03 AM
The `c:\NTHU\Junior_I\Data Lab\Cliffhanger_app\cliffhanger_app\src\services\api\client.ts` file, an HTTP client configuration, underwent a single significant update.

The initial version of the file, timestamped `4/26/2026, 6:31:48 PM`, defined the development host (`DEV_HOST`) as `'10.1.160.207'`.

A subsequent update to the same file, timestamped `4/26/2026, 9:38:19 PM`, changed the `DEV_HOST` to `'172.20.10.2'`.

In both versions, the file consistently configures the `BASE_URL` based on the platform (`web` uses `localhost`, others use `DEV_HOST`). It provides a generic `request` function that handles JSON content types, adds `Bearer` token authorization if provided, and performs error checking. The `apiClient` object exports `get` and `post` methods, both utilizing the `request` function to interact with the API.

The key pattern observed is the consistent structure and functionality of the API client throughout these changes, with the only modification being the adjustment of the `DEV_HOST` IP address, likely for connecting to a different development backend instance.

## 2:33:52 PM
The `c:\NTHU\Junior_I\Data Lab\Cliffhanger_app\cliffhanger_app\src\services\api\client.ts` file, which defines an HTTP client for API interactions, was updated.

The initial version of the file, timestamped 4/26/2026, 6:31:48 PM, set the `DEV_HOST` constant to `'10.1.160.207'`. This constant is used to construct the `BASE_URL` for non-web platforms, facilitating connections to a backend server.

A subsequent change, recorded at 4/26/2026, 9:38:19 PM, involved a single modification within the same file: the `DEV_HOST` IP address was updated from `'10.1.160.207'` to `'172.20.10.2'`. All other aspects of the API client, including the `request` function, header handling (for `Content-Type` and `Authorization` tokens), and the `apiClient` methods (`get` and `post`), remained identical between the two timestamps.

The key change is the adjustment of the development host IP address, indicating a probable network configuration update or a shift in the local backend server's address for testing purposes.

## 3:33:55 PM
The log primarily details changes to a single file, `c:\NTHU\Junior_I\Data Lab\Cliffhanger_app\cliffhanger_app\src\services\api\client.ts`, which serves as the HTTP client for the application.

**File-specific updates for `c:\NTHU\Junior_I\Data Lab\Cliffhanger_app\cliffhanger_app\src\services\api\client.ts`:**
The file defines an `apiClient` for making API requests. It includes:
*   Configuration for `BASE_URL`, dynamically set based on the platform (`web` uses `localhost`, others use a `DEV_HOST`).
*   A generic `request` function that handles:
    *   Constructing the full URL.
    *   Setting `Content-Type: application/json` header.
    *   Adding `Authorization: Bearer <token>` header if a token is provided.
    *   Handling HTTP errors by throwing an `Error` with the status and response body.
    *   Parsing the JSON response.
*   Exported `apiClient` methods for `get` and `post` requests, leveraging the `request` function. The `post` method automatically stringifies the request body.

The key change across the two log entries for this file is an update to the `DEV_HOST` constant, which is used for non-web platforms (e.g., physical devices, Android emulator).

**Timestamps of significant changes:**
*   **4/26/2026, 6:31:48 PM**: The initial version of the HTTP client was established, setting `DEV_HOST` to `'10.1.160.207'`.
*   **4/26/2026, 9:38:19 PM**: The `DEV_HOST` IP address was updated from `'10.1.160.207'` to `'172.20.10.2'`. This indicates a configuration change likely due to a change in the development server's network address or a shift in the testing environment.

**Patterns or recurring elements:**
The overall structure and logic of the `apiClient` module remain consistent across both versions. This includes:
*   The conditional `BASE_URL` setup using `Platform.OS`.
*   The `request` function's robust error handling and token-based authentication.
*   The `apiClient` export providing `get` and `post` wrappers.
The only element that changed was the specific IP address used for `DEV_HOST`, while its role and how it's used in constructing `BASE_URL` remained constant.

## 4:33:54 PM
The provided log details changes to a single file: `c:\NTHU\Junior_I\Data Lab\Cliffhanger_app\cliffhanger_app\src\services\api\client.ts`.

**File-Specific Updates:**

*   **`c:\NTHU\Junior_I\Data Lab\Cliffhanger_app\cliffhanger_app\src\services\api\client.ts`**: This file defines an HTTP client for the application's API.
    *   **Initial Configuration (4/26/2026, 6:31:48 PM)**: The `DEV_HOST` constant was set to `'10.1.160.207'`. The `BASE_URL` was configured to use this host for non-web platforms and `localhost` for web. The file established a generic `request` function that handles API calls, setting `Content-Type` and `Authorization` headers (if a token is provided), and parsing JSON responses or throwing errors for non-OK statuses. It also exported an `apiClient` object with `get` and `post` methods that utilize this `request` function.
    *   **Subsequent Update (4/26/2026, 9:38:19 PM)**: The only change made was the update of the `DEV_HOST` constant from `'10.1.160.207'` to `'172.20.10.2'`. The underlying logic for API requests and client methods remained identical.

**Timestamps of Significant Changes:**

*   **4/26/2026, 6:31:48 PM**: Initial implementation of the API client, setting up the base URL logic and request handling.
*   **4/26/2026, 9:38:19 PM**: A specific configuration update, changing the `DEV_HOST` IP address.

**Patterns or Recurring Elements:**

*   **Consistent API Client Logic**: The core structure and functionality of the `apiClient` (including the `request` function, header handling, error management, and `get`/`post` methods) remained unchanged across both timestamps.
*   **Dev Host IP Configuration**: The repeated modification of the `DEV_HOST` IP address suggests ongoing adjustments or refinements to the development server's network configuration, likely to accommodate different testing environments or network setups. The `BASE_URL` dynamically adapts based on the platform (`web` vs. other), using `localhost` for web and the `DEV_HOST` for native environments.