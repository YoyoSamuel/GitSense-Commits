# Activity Summary for 5/7/2026

## 1:51:56 PM
The project log indicates changes related to a "RoyalRoad Reader-Comment Generator" pipeline, designed to scrape web novel chapters and comments, process them, fine-tune a Large Language Model (LLM), and generate reader comments.

**File-specific updates:**

*   **`c:\NTHU\Junior_I\Data Lab\RoyalRoad\.gitignore` (5/7/2026, 11:52:18 AM):**
    This file was updated to include common ignored files for a Python project. It specifically adds `__pycache__/`, `*.pyc`, `.venv/`, `.python-version`, `uv.lock`, `checkpoints/`, `sft_data/`, `sft_data.zip`, `wandb/`, `*.jsonl`, and `mystory*.txt`. This indicates an initial setup for version control, ensuring build artifacts, virtual environments, large data files, model checkpoints, and temporary logs are not committed.

*   **`c:\NTHU\Junior_I\Data Lab\RoyalRoad\README.md` (5/7/2026, 12:06:35 PM and 5/7/2026, 12:06:47 PM):**
    The `README.md` underwent two rapid updates, suggesting an initial detailed documentation commit followed by a quick correction.
    *   **Initial comprehensive update (5/7/2026, 12:06:35 PM):** This extensive `README` introduces the project's purpose: scraping RoyalRoad, processing data, fine-tuning a Qwen-based LLM with LoRA, and generating reader comments. It includes a pipeline diagram, quick start inference instructions, and a detailed breakdown of each of the six core scripts:
        1.  `chapter_scraper.py`: Scrapes chapter text to `chapters_dataset.jsonl`.
        2.  `comment_scraper.py`: Scrapes reader comments to `comments_dataset.jsonl`.
        3.  `merge_comments.py`: Distills noisy comments using an Ollama-cloud model (`gemma4:31b-cloud`) into single-voice entries in `comments_merged.jsonl`. This step requires an `OLLAMA_API_KEY`.
        4.  `prepare_sft_data.py`: Joins chapters and merged comments to build chat-format SFT examples in `sft_data/train.jsonl`.
        5.  `train_comment_sft.py`: Fine-tunes a Qwen base model (Qwen3.5-9B) using QLoRA via TRL's `SFTTrainer`, saving the final adapter to `checkpoints/comment_sft/final/`.
        6.  `generate_comments.py`: Generates comments using the fine-tuned model.
        The `README` also covers setting up the Hugging Face cache and provides guidance on sharing checkpoints.
    *   **Minor correction (5/7/2026, 12:06:47 PM):** Shortly after the initial detailed commit, the `README.md` was updated to correct the Hugging Face repository name for downloading the LoRA adapter in the "Quick start (inference only)" section. The repository name was changed from `Sam/royalroad-comment-sft` to `SamuelYo/royalroad-comment-sft`.

**Timestamps of significant changes:**
The changes occurred on a single date, May 7, 2026, within a short span, indicating focused initial setup and documentation efforts. The `.gitignore` was established first (11:52:18 AM), followed by the comprehensive `README.md` (12:06:35 PM), and then a quick fix to the `README.md` (12:06:47 PM).

**Patterns or recurring elements:**
*   **Data Flow:** The project follows a clear data pipeline: scrape -> process -> train -> generate.
*   **File Naming Conventions:** Datasets are consistently named `*_dataset.jsonl` or `*_merged.jsonl`, and training data is `sft_data/train.jsonl`.
*   **LLM Fine-tuning:** The core of the project involves fine-tuning a Qwen-based LLM with LoRA.
*   **Hugging Face Integration:** Hugging Face Hub is central for model management, including downloading base models and sharing LoRA adapters.
*   **Detailed Instructions:** All scripts are accompanied by clear command-line usage examples and explanations of their purpose, inputs, and outputs.
*   **Resume/Idempotency:** Scraper scripts (`chapter_scraper.py`, `comment_scraper.py`, `merge_comments.py`) are designed to resume from existing output, preventing redundant work.
*   **Resource Management:** Mentions of GPU memory requirements (e.g., ~24 GB for training) and polite scraping (2-second request delay) indicate attention to practical deployment.

## 4:52:00 PM
The `.gitignore` file was initialized on May 7, 2026, at 11:52:18 AM, establishing rules to exclude common Python build artifacts (`__pycache__`, `*.pyc`), virtual environments (`.venv`, `.python-version`), dependency lock files (`uv.lock`), and critically, generated training data and model outputs (`checkpoints/`, `sft_data/`, `sft_data.zip`, `wandb/`, `*.jsonl`, `mystory*.txt`). This indicates a clear intention to manage version control by keeping large, ephemeral, or sensitive build products out of the repository.

Shortly after, two rapid updates were made to the `README.md` file on May 7, 2026, at 12:06:35 PM and 12:06:47 PM.
The initial `README.md` update (12:06:35 PM) introduced a comprehensive overview of the "RoyalRoad Reader-Comment Generator" project. This included:
*   A pipeline description: scraping chapters and comments, distilling data, fine-tuning a Qwen-based LLM with LoRA, and generating reader comments.
*   A visual pipeline diagram showing the flow between `chapter_scraper.py`, `comment_scraper.py`, `prepare_sft_data.py`, `merge_comments.py`, `train_comment_sft.py`, and `generate_comments.py`.
*   Detailed "Quick start" instructions for inference, covering setup, Hugging Face login, `HF_HOME` configuration, downloading the LoRA adapter (initially from `Sam/royalroad-comment-sft`), and running `generate_comments.py` with various parameters.
*   A full breakdown of each pipeline script, specifying its purpose, usage examples, output schemas (e.g., `chapters_dataset.jsonl`, `comments_dataset.jsonl`, `comments_merged.jsonl`, `sft_data/train.jsonl`), and important considerations like resume capabilities, API key requirements (`OLLAMA_API_KEY` for `merge_comments.py`), and GPU usage.
*   Instructions for LoRA fine-tuning with `train_comment_sft.py`, including model details (Qwen3.5-9B), quantization (4-bit), and checkpoint outputs.
*   A section on sharing checkpoints via Hugging Face Hub.

The subsequent `README.md` update (12:06:47 PM) was a minor but specific correction to the "Quick start" section. It changed the Hugging Face repository ID for downloading the LoRA adapter from `Sam/royalroad-comment-sft` to `SamuelYo/royalroad-comment-sft`. The very short time difference (12 seconds) between the two `README.md` updates suggests an immediate correction or refinement during the initial documentation phase.

**Patterns and Recurring Elements:**
*   **LLM Fine-tuning Pipeline:** The core theme is an end-to-end pipeline for LLM fine-tuning and inference, specifically for generating reader comments.
*   **Hugging Face Integration:** Hugging Face Hub is central for base model and adapter management (downloading, uploading, specifying IDs).
*   **Modular Python Scripts:** The project is structured around distinct Python scripts, each responsible for a specific stage of the data processing or model lifecycle.
*   **Data Management:** Emphasis on structured data output (JSONL files with specified schemas), data preparation, and filtering noise in comments.
*   **Resource Awareness:** Mentions of GPU requirements, 4-bit quantization, and `HF_HOME` indicate consideration for system resources and performance.
*   **Clear Documentation Practices:** The `README.md` provides extensive detail, command examples, and explanations, demonstrating a strong commitment to clear project documentation.
*   **Exclusion of Generated Artifacts:** The `.gitignore` reinforces the project's strategy of keeping generated data and model checkpoints out of version control, which is also reflected in the `README.md`'s explanation of pipeline outputs.

## 6:51:55 PM
A `.gitignore` file was created or updated at `5/7/2026, 11:52:18 AM`. This file defines common exclusions for a Python project, including `__pycache__`, compiled Python files (`.pyc`), virtual environment directories (`.venv/`), `uv.lock`, and project-specific outputs such as `checkpoints/`, `sft_data/`, `sft_data.zip`, `wandb/`, all `.jsonl` files, and `mystory*.txt` files.

The `README.md` file underwent two rapid updates, first at `5/7/2026, 12:06:35 PM` and again at `5/7/2026, 12:06:47 PM`.
The `README.md` describes a "RoyalRoad Reader-Comment Generator" pipeline designed to scrape chapters and comments from RoyalRoad, process them into training data, fine-tune a Qwen-based LLM using LoRA, and generate new reader comments.

Key details from the `README.md` include:
*   **Pipeline Overview:** A six-step process involving `chapter_scraper.py`, `comment_scraper.py`, `merge_comments.py`, `prepare_sft_data.py`, `train_comment_sft.py`, and `generate_comments.py`.
*   **Data Flow:** The pipeline generates and consumes several `.jsonl` files, such as `chapters_dataset.jsonl`, `comments_dataset.jsonl`, `comments_merged.jsonl`, and `sft_data/train.jsonl`.
*   **Comment Merging:** The `merge_comments.py` script is highlighted for its role in distilling noisy raw comments into single-voice, casual comments using an Ollama-cloud model (`gemma4:31b-cloud`), requiring an `OLLAMA_API_KEY` environment variable.
*   **LLM Fine-tuning:** `train_comment_sft.py` fine-tunes a `Qwen/Qwen3.5-9B` model using QLoRA, saving the adapter to `checkpoints/comment_sft/final/`. It notes that a single ~24 GB GPU is sufficient for the default setup and requires setting `HF_HOME`.
*   **Comment Generation:** `generate_comments.py` is used for inference, capable of using either the base model or the fine-tuned LoRA adapter.
*   **Hugging Face Integration:** The project heavily relies on Hugging Face for model hosting, providing instructions for logging in, downloading adapters, and uploading trained checkpoints.
*   **Timestamps of Significance:** The rapid updates to `README.md` within a span of 12 seconds (from `12:06:35 PM` to `12:06:47 PM`) indicate a quick correction or refinement.
*   **Specific Update:** The second update to `README.md` at `12:06:47 PM` was a minor but critical correction to the Hugging Face repository ID for downloading the LoRA adapter, changing it from `Sam/royalroad-comment-sft` to `SamuelYo/royalroad-comment-sft` in the "Quick start (inference only)" section.

**Patterns and Recurring Elements:**
*   Extensive use of `.jsonl` files for storing structured data throughout the pipeline.
*   Heavy reliance on environment variables for sensitive information (e.g., `OLLAMA_API_KEY`) and configuration (e.g., `HF_HOME`).
*   Clear emphasis on a multi-stage data processing and machine learning pipeline, common in LLM fine-tuning projects.
*   Detailed command-line examples and explanations for each script, facilitating reproducibility and ease of use.
*   The project prioritizes politeness in scraping (e.g., `REQUEST_DELAY`) and supports resuming operations to prevent redundant work.