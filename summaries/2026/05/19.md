# Activity Summary for 5/19/2026

## 1:06:18 AM
The changes primarily focus on implementing and integrating a conservative concurrency control mechanism within the VanillaDB system. All modifications occurred on May 18, 2026, within a concentrated timeframe between 2:34 PM and 3:22 PM. A recurring pattern is the use of `// new implement` comments to mark new or modified sections of code.

Here's a breakdown of the key information by file:

**`c:\NTHU\Junior_II\DBSM2\hw5\hw5\core-patch\src\main\java\org\vanilladb\core\storage\tx\concurrency\conservative\PrimaryKey.java`**
This file defines a `PrimaryKey` class used for identifying specific data items (records) based on their table name and key-value pairs.
*   **5/18/2026, 2:34:38 PM:** The initial version of the `PrimaryKey` class was introduced, featuring `tableName` and `keyEntryMap`, along with standard object methods (`equals`, `hashCode`, `toString`) and a method to return an unmodifiable `keyEntryMap`.
*   **5/18/2026, 2:42:53 PM:** The `toString()` method and `getKeyEntryMap()` method were removed, and the `hashCode()` implementation was slightly adjusted.
*   **5/18/2026, 2:46:43 PM - 5/18/2026, 2:55:55 PM:** Subsequent entries for this file show no functional code changes, only minor comment additions like `// modified` or `// new implement` at the top of the file, suggesting iterative saves during development without altering the core logic established at 2:42:53 PM.

**`c:\NTHU\Junior_II\DBSM2\hw5\hw5\core-patch\src\main\java\org\vanilladb\core\storage\tx\concurrency\ConservativeConcurrencyMgr.java`**
This file defines a concurrency manager implementing conservative locking.
*   **5/18/2026, 2:54:35 PM:** The `ConservativeConcurrencyMgr` class was initially a skeleton with all overridden `ConcurrencyMgr` methods marked as `// TODO`, indicating placeholders.
*   **5/18/2026, 2:56:49 PM:** A substantial update occurred, fully implementing the conservative locking logic. This involved:
    *   Adding necessary imports for `Collections`, `HashSet`, `Set`, and the new `PrimaryKey` class.
    *   Introducing a constructor that sets the transaction number (`txNum`).
    *   Implementing a `prepareSp` method, which takes read and write sets of `PrimaryKey` objects and acquires exclusive locks (`xLock`) for write items and shared locks (`sLock`) for read-only items using a mutex to ensure atomicity of lock acquisition.
    *   All placeholder methods were replaced with concrete implementations that acquire appropriate locks (e.g., `xLock`, `sLock`, `isLock`, `ixLock`) on files, blocks, or records using a `lockTbl` object.
    *   `onTxEndStatement` was explicitly marked to do nothing, as conservative locks are held until transaction commit or rollback.
*   **5/18/2026, 2:57:02 PM:** Only a comment `// new implement` was added to the top of the file.

**`c:\NTHU\Junior_II\DBSM2\hw5\hw5\core-patch\src\main\java\org\vanilladb\core\sql\storedprocedure\StoredProcedureParamHelper.java`**
This abstract helper class for stored procedures was modified to support explicit declaration of read/write sets for concurrency control.
*   **5/18/2026, 3:01:11 PM:** The file contained abstract methods for parameter preparation, result schema, and result record creation, along with `isReadOnly` flag.
*   **5/18/2026, 3:01:17 PM - 3:01:49 PM:** Imports for `Collections`, `Set`, and `PrimaryKey` (with a corrected import path to `org.vanilladb.core.storage.tx.concurrency.conservative.PrimaryKey`) were added.
*   **5/18/2026, 3:02:53 PM - 3:03:35 PM:** New methods `getReadSet()` and `getWriteSet()` were introduced, initially returning empty sets, to allow stored procedures to declare their data access requirements. Comments clarified their purpose for concurrency control using primary keys.

**`c:\NTHU\Junior_II\DBSM2\hw5\hw5\core-patch\src\main\java\org\vanilladb\core\sql\storedprocedure\StoredProcedure.java`**
This abstract class for stored procedures was updated to integrate with the conservative concurrency manager.
*   **5/18/2026, 3:04:17 PM:** The `ConservativeConcurrencyMgr` was imported.
*   **5/18/2026, 3:05:15 PM - 3:05:34 PM:** Comments were added to the `execute()` method's `try` block, signaling the intent to add concurrency control logic.
*   **5/18/2026, 3:05:49 PM - 3:07:29 PM:** The `execute()` method was modified to check if the transaction's concurrency manager is an instance of `ConservativeConcurrencyMgr`. If so, it casts the manager and calls `ccMgr.prepareSp()`, passing the read and write sets obtained from the `paramHelper`. This ensures that required locks are acquired before the stored procedure's SQL execution.

**`c:\NTHU\Junior_II\DBSM2\hw5\hw5\bench\src\main\java\org\vanilladb\bench\server\param\micro\MicroTxnProcParamHelper.java`**
This specific parameter helper for micro-transactions was adapted to provide concrete read/write sets.
*   **5/18/2026, 3:13:22 PM:** Initial version of the `MicroTxnProcParamHelper` included fields for read/write item IDs and prices, and methods for parameter preparation and result set handling.
*   **5/18/2026, 3:13:43 PM - 3:14:33 PM:** The import path for `PrimaryKey` was corrected to `org.vanilladb.core.storage.tx.concurrency.conservative.PrimaryKey`, and comments indicating "new implement" were added.
*   **5/18/2026, 3:16:04 PM:** New `readSet` and `writeSet` `HashSet` fields were introduced, along with a `makeItemKey` helper method to create `PrimaryKey` objects for items. The `getReadSet()` and `getWriteSet()` methods were overridden to return these new sets.
*   **5/18/2026, 3:17:32 PM - 5/18/2026, 3:22:51 PM:** The `prepareParameters` method was significantly updated to populate the `readSet` and `writeSet` fields. It iterates through the `readItemId` and `writeItemId` arrays, using `makeItemKey` to create `PrimaryKey` objects for each item and adding them to the respective sets. This enables the micro-transaction to declare its locking requirements upfront.

**`c:\NTHU\Junior_II\DBSM2\hw5\hw5\bench\src\main\resources\org\vanilladb\core\vanilladb.properties`** and **`c:\NTHU\Junior_II\DBSM2\hw5\hw5\core-patch\src\test\resources\org\vanilladb\core\vanilladb.properties`**
These configuration files specify various settings for VanillaDB.
*   **5/18/2026, 3:21:19 PM** (bench) and **5/18/2026, 3:21:44 PM** (core-patch test): Both files were updated to configure the `SERIALIZABLE_CONCUR_MGR` property. This property is now set to `org.vanilladb.core.storage.tx.concurrency.ConservativeConcurrencyMgr`, indicating that the newly implemented conservative concurrency manager will be used for transactions with serializable isolation levels in both the benchmark and core test environments.

In summary, the changes demonstrate a comprehensive effort to introduce and integrate a conservative concurrency control mechanism into the VanillaDB project. This involved defining a `PrimaryKey` for fine-grained locking, creating a `ConservativeConcurrencyMgr` that pre-acquires locks based on declared read/write sets, extending `StoredProcedureParamHelper` to expose these sets, and modifying `StoredProcedure` to utilize this new concurrency manager during execution. Finally, the system configuration files were updated to enable the `ConservativeConcurrencyMgr` for serializable transactions.