# Activity Summary for 5/29/2026

## 12:49:27 AM
**`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\contexts\AuthContext.tsx` (Timestamp: 5/28/2026, 2:23:45 AM)**
This file defines the `AuthContext` responsible for managing user authentication state across the application. It includes the user object, access token, and a loading status. Key functionalities involve:
*   Providing functions to set, clear, and retrieve the access token.
*   Implementing logic to check JWT token expiration (`getJwtExpSeconds`, `isAccessTokenFresh`).
*   Attempting to recover user sessions from a stored refresh token in `localStorage` upon initial mount.
*   Handling token refreshes via the `apiRefresh` call when the current access token is stale.
*   Explicitly noting a design choice to store refresh tokens in `localStorage` for simplified deployment, acknowledging the security trade-offs compared to HTTP-only cookies.

**`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ExamManagePage.tsx` (Timestamp: 5/28/2026, 8:03:23 PM)**
This page provides an interface for administrators to create and edit exams.
*   It manages exam details such as title, description, start/end times, and whether to show scores.
*   Utilizes the `useAuth` hook to retrieve access tokens for authenticated API calls.
*   Fetches lists of all available problems and candidate users for selection.
*   For existing exams, it retrieves current exam details and assignments.
*   Includes functions to `handleSave` (creating or updating an exam), `handleDelete` an exam, and `handleAddAssignment`/`handleRemoveAssignment` for managing problems assigned to candidates within an exam.
*   The UI features input fields, date-time pickers, checkboxes, and filterable lists for problems (by search and difficulty) and candidates (by search and role).

**`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\UserManagement.tsx` (Timestamp: 5/28/2026, 8:05:11 PM)**
This component presents an administrative panel for managing users.
*   It displays a paginated list of admin users, showing their name, email, role, status (active/inactive), and creation date.
*   Allows searching users by name or email and filtering by user role (`admin`, `interviewer`, `problem_admin`, `candidate`).
*   Provides actions to navigate to user creation/edit pages, and functions to `handleSave` user updates, `handleDeactivate` users, and `handleDelete` users.
*   The UI includes search input, a role filter dropdown, pagination controls, and "Edit" / "Delete" buttons for each user entry in the table.

**`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\api\admin.ts` (Timestamps: 5/28/2026, 8:17:41 PM; 5/28/2026, 8:18:21 PM; 5/28/2026, 8:18:38 PM)**
This file serves as the API client for administrative operations.
*   It defines asynchronous functions for interacting with `/api/v1/admin/users` and `/api/v1/admin/exams` endpoints.
*   **Key functions include:**
    *   `apiListAdminUsers`: Retrieves a list of admin users, supporting pagination, role, and name filtering.
    *   `apiCreateAdminUser`: Creates a new admin user.
    *   `apiUpdateAdminUser`: Updates an existing admin user's details.
    *   `apiGetAdminUser`: Fetches details for a specific admin user.
    *   `apiDeleteAdminUser`: Deletes an admin user.
    *   `apiGetExamResults`: Fetches exam results for a given exam ID.
*   All requests include an `Authorization` header with a bearer token.
*   A notable change occurs between 8:17:41 PM and 8:18:21 PM, where the custom `throwOnError` helper is refactored to use a more generic `throwOnApiError` imported from `./errors`, centralizing error handling logic. The content from 8:18:21 PM and 8:18:38 PM is identical, suggesting minor or incidental saves.

**Patterns and Recurring Elements:**
*   **Timestamp Concentration:** All significant file changes occurred on May 28, 2026, indicating concentrated development effort, particularly in the evening.
*   **Authentication Flow:** The `AuthContext` provides a centralized authentication mechanism, with access tokens retrieved via `getAccessToken` being consistently used across different pages (`ExamManagePage`, `UserManagement`) for API authorization.
*   **API Interaction Pattern:** Frontend components frequently use `useEffect` to fetch initial data or react to token changes, and `async`/`await` syntax for handling API calls. Error states are commonly managed using `useState` variables.
*   **Consistent Styling:** A shared `inputCls` variable is used across `ExamManagePage` and `UserManagement` to maintain a uniform look for input fields.
*   **Common UI Patterns:** Loading indicators, error messages, search functionalities, filtering options, pagination, and explicit action buttons (e.g., Edit, Delete, Save) are recurring elements across the management pages.
*   **Centralized API Error Handling:** The transition in `src/api/admin.ts` to `throwOnApiError` demonstrates a move towards more consistent and reusable error handling for API responses.

## 3:08:33 AM
The provided code changes primarily focus on enhancing the frontend functionality for an online judge (OJ) system, specifically around user authentication and administrative management of exams and users.

**File-specific Updates:**

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\contexts\AuthContext.tsx` (Timestamp: 5/28/2026, 2:23:45 AM)**
    This file establishes the core authentication context for the frontend. It defines an `AuthContext` to manage user sessions, including `user` details, `accessToken`, and a `loading` state during initial authentication checks. Key functionalities include:
    *   `setAuth`: Stores user data, access token, and refresh token (in `localStorage`).
    *   `clearAuth`: Clears authentication data and performs an API logout.
    *   `getAccessToken`: Provides a mechanism to retrieve a fresh access token, attempting to refresh it using the stored refresh token if the current one is expired or missing.
    *   Helper functions `getJwtExpSeconds` and `isAccessTokenFresh` are implemented to validate JWT expiration.
    *   An `useEffect` hook ensures session recovery from `localStorage` upon component mount.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ExamManagePage.tsx` (Timestamp: 5/28/2026, 8:03:23 PM)**
    This page introduces comprehensive management capabilities for exams. It allows for the creation and editing of exam details.
    *   Users can set an exam's `title`, `description`, `start_time`, `end_time`, and a `show_score` flag.
    *   It integrates with various API calls (`apiCreateExam`, `apiUpdateExam`, `apiDeleteExam`, `apiListProblems`, `apiListAdminUsers`, `apiCreateAssignment`, `apiDeleteAssignment`) to fetch exam data, manage problems, candidates, and assignments.
    *   The page dynamically loads existing exam data for editing or presents a blank form for new exams.
    *   It includes input fields for searching and filtering problems by difficulty, and for selecting candidates, alongside buttons to save or delete exams and manage problem/candidate assignments.
    *   A utility function `toLocalDatetime` is defined for formatting date-time inputs.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\UserManagement.tsx` (Timestamp: 5/28/2026, 8:05:11 PM)**
    This component provides an administrative interface for managing users within the system.
    *   It displays a paginated list of users, showing their `name`, `email`, `role`, `status` (active/inactive), and `created_at` date.
    *   Functionality includes searching users by name/email and filtering by role (`admin`, `interviewer`, `problem_admin`, `candidate`).
    *   Users can be created, edited, deactivated, or deleted through associated API calls (`apiListAdminUsers`, `apiCreateAdminUser`, `apiUpdateAdminUser`, `apiDeactivateAdminUser`, `apiDeleteAdminUser`).
    *   The page features loading states and error message displays.
    *   A `formatDate` utility function is included for consistent date presentation.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\api\admin.ts` (Timestamps: 5/28/2026, 8:17:41 PM; 8:18:21 PM; 8:18:38 PM)**
    This file serves as the API client for administrative operations, primarily for managing users and fetching exam results.
    *   It defines a set of asynchronous functions (`apiListAdminUsers`, `apiCreateAdminUser`, `apiUpdateAdminUser`, `apiGetAdminUser`, `apiDeleteAdminUser`, `apiGetExamResults`) for interacting with `/admin` endpoints.
    *   **Significant Change Pattern**: The initial version (8:17:41 PM) implemented a local `throwOnError` helper function. In subsequent updates (8:18:21 PM and 8:18:38 PM), this local function was refactored to utilize a shared `throwOnApiError` function imported from `./errors`, indicating a move towards centralized and standardized error handling across API calls. The core functionality of the API calls remained consistent across these three rapid updates.

**Overall Patterns and Recurring Elements:**

*   **Authentication Integration**: All pages and API calls requiring user context (`ExamManagePage`, `UserManagement`, `api\admin.ts`) consistently rely on retrieving an access token via `useAuth` or passing it directly.
*   **API-driven Frontend**: The frontend components are heavily dependent on API interactions for data fetching, creation, updating, and deletion operations, using a structured approach with dedicated API client files.
*   **State Management with Hooks**: React's `useState` and `useEffect` hooks are extensively used for managing component local state, side effects, loading indicators, and error handling across different pages.
*   **Consistent UI Styling**: A recurring `inputCls` variable is used to apply consistent Tailwind CSS styles to input fields across `ExamManagePage` and `UserManagement`, indicating a standardized design system.
*   **Error Handling**: A clear pattern of handling potential API errors by catching exceptions and displaying user-friendly error messages is observed throughout the application logic. The refactoring in `admin.ts` specifically points to an evolving, more robust error handling strategy.
*   **Date Formatting Utilities**: Custom utility functions for formatting dates and times (`toLocalDatetime`, `formatDate`) are implemented to ensure consistent data presentation.

## 6:17:56 AM
The code changes primarily focus on frontend development, specifically related to authentication and administrative functionalities for managing exams and users.

**File-Specific Updates:**

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\contexts\AuthContext.tsx`** (Timestamp: 5/28/2026, 2:23:45 AM)
    *   Introduces an `AuthContext` and `AuthProvider` to manage user authentication state throughout the application.
    *   It handles `user`, `accessToken`, and loading states.
    *   Key features include:
        *   Functions (`getJwtExpSeconds`, `isAccessTokenFresh`) for JWT token validation and expiration checks.
        *   Session recovery on mount using a stored `refresh_token` via `apiRefresh` and `apiMe`.
        *   `setAuth` to store new authentication details (user, access, and refresh tokens).
        *   `clearAuth` to log out a user and remove tokens.
        *   `getAccessToken` to provide a current access token, refreshing it if necessary using the stored refresh token.
    *   This file establishes the core authentication mechanism for the frontend.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ExamManagePage.tsx`** (Timestamp: 5/28/2026, 8:03:23 PM)
    *   Implements a comprehensive page for managing exams.
    *   It supports creating new exams and editing existing ones, including updating title, description, start/end times, and whether scores are shown.
    *   Allows administrators to manage assignments within an exam by selecting problems (with search and difficulty filters) and candidates from a list.
    *   Integrates with various API calls for exam and assignment CRUD operations (`apiCreateAssignment`, `apiCreateExam`, `apiDeleteAssignment`, `apiDeleteExam`, `apiGetExam`, `apiListAssignments`, `apiListProblems`, `apiListAdminUsers`).
    *   Features UI elements for input fields, date/time pickers, checkboxes, search, filters, and action buttons (Save, Delete, Add/Remove Assignment).

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\UserManagement.tsx`** (Timestamp: 5/28/2026, 8:05:11 PM)
    *   Provides an administrative interface for managing users.
    *   Displays a paginated list of users with details like name, email, role, status, and creation date.
    *   Includes functionality for searching users by name/email and filtering by role.
    *   Features actions to edit user roles/details and deactivate users (`handleSave`, `handleDeactivate`). A "Delete" button is present in the UI but the associated `handleDelete` function implementation is not fully shown in the provided snippet, although `apiDeleteAdminUser` is referenced.
    *   Relies on the `useAuth` hook for authentication and `apiListAdminUsers` for fetching user data.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\api\admin.ts`** (Timestamps: 5/28/2026, 8:17:41 PM, 8:18:21 PM, 8:18:38 PM)
    *   This file defines the API client functions for interacting with admin-related endpoints.
    *   It includes functions for:
        *   `apiListAdminUsers`: Fetching a list of admin users with filtering and pagination options.
        *   `apiCreateAdminUser`: Creating a new admin user.
        *   `apiUpdateAdminUser`: Updating an existing admin user's details.
        *   `apiGetAdminUser`: Retrieving details for a specific admin user.
        *   `apiDeleteAdminUser`: Deleting an admin user.
        *   `apiGetExamResults`: Retrieving exam results for a given exam ID.
    *   A significant pattern change occurred between 8:17:41 PM and 8:18:21 PM where the `throwOnError` utility function was refactored to import and use a centralized `throwOnApiError` from `./errors`, indicating a move towards consistent API error handling. The 8:18:38 PM entry is identical to the one at 8:18:21 PM, suggesting no further changes in that exact minute.

**Timestamps of Significant Changes:**

*   **5/27/2026, 10:17:59 PM**: Initial setup or modification of environment variables (skipped from summary as per instruction).
*   **5/28/2026, 2:23:45 AM**: Core authentication context (`AuthContext.tsx`) was defined, laying the groundwork for user sessions.
*   **5/28/2026, 8:03:23 PM - 8:05:11 PM**: Rapid development of administrative user interfaces (`ExamManagePage.tsx` and `UserManagement.tsx`), indicating significant progress on core management features.
*   **5/28/2026, 8:17:41 PM - 8:18:38 PM**: Quick iteration on the `admin.ts` API client, especially a refactoring of the error handling mechanism, suggesting immediate refinement after implementing the admin pages.

**Patterns or Recurring Elements:**

*   **Frontend Focus:** All summarized changes are within the `frontend` directory, indicating active development on the client-side application.
*   **React/TypeScript Usage:** The codebase heavily utilizes React hooks (`useState`, `useEffect`, `useCallback`, `useContext`) and TypeScript for structured and type-safe development.
*   **Authentication Integration:** Components frequently rely on a centralized `AuthContext` and `useAuth` hook to manage user sessions and retrieve access tokens for authenticated API calls.
*   **API Interaction Pattern:** There's a consistent pattern of defining API functions that take a `token` and payload, perform `fetch` requests to specific `/api/v1` endpoints, and include a standardized error handling mechanism (`throwOnError`/`throwOnApiError`).
*   **Administrative Features:** A strong theme is the development of administrative tools, specifically for managing exams (creation, assignments) and users (listing, filtering, CRUD operations).
*   **Styling:** A common `inputCls` variable is defined and reused across different pages, suggesting a consistent styling approach for form inputs.
*   **Error Handling Refinement:** The rapid changes in `api/admin.ts` demonstrate a pattern of centralizing and refining API error handling.

## 9:07:04 PM
The changes primarily focus on the frontend application, enhancing authentication, exam management, and user management features. All notable modifications occurred on **May 28, 2026**.

**File-Specific Updates:**

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\contexts\AuthContext.tsx` (Timestamp: 5/28/2026, 2:23:45 AM)**
    *   This file establishes the core authentication context for the React application.
    *   It defines `AuthContext` and `AuthProvider` to manage user sessions, access tokens, and loading states.
    *   It implements functionality to recover user sessions from a stored refresh token in `localStorage` on component mount, utilizing `apiRefresh` and `apiMe` calls.
    *   Includes helper functions (`getJwtExpSeconds`, `isAccessTokenFresh`) for JWT token validation and freshness checks.
    *   `setAuth` and `clearAuth` functions are provided for updating and clearing authentication states, respectively, with `clearAuth` also performing `apiLogout`.
    *   `getAccessToken` ensures that an access token is fresh, refreshing it if necessary using a stored refresh token.
    *   A significant design note explains the decision to store refresh tokens in `localStorage` as a trade-off for a single-machine deployment, prioritizing ease of handling over stricter XSS protection offered by HTTP-only cookies.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ExamManagePage.tsx` (Timestamp: 5/28/2026, 8:03:23 PM)**
    *   This component provides a comprehensive interface for creating and editing exams.
    *   It uses `react-router-dom` for navigation and `useAuth` to retrieve the access token.
    *   Manages a wide array of states including exam details, problems, candidates, loading, saving, and errors.
    *   Fetches existing exam data, problems, and administrative users (candidates) upon initialization for existing exams, or prepares an empty form for new ones.
    *   Includes functions to handle saving (creating or updating), deleting exams, and adding/removing assignments (problems and candidates).
    *   The UI features an input form for exam title, description, start/end times, and a "show score" checkbox. It also includes sections with search and filter capabilities for problems and candidates, allowing selection of which problems and candidates are part of the exam.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\UserManagement.tsx` (Timestamp: 5/28/2026, 8:05:11 PM)**
    *   This component offers an administrative view for managing users.
    *   It displays a paginated list of admin users with filtering options by role and a search function by name or email.
    *   Fetches user data using `apiListAdminUsers` and provides functionality to navigate to user creation/edit pages, and to deactivate or delete users.
    *   Displays user information such as name, email, role, active status, and creation date in a table format.
    *   Includes pagination controls for navigating through user lists.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\api\admin.ts` (Timestamps: 5/28/2026, 8:17:41 PM, 8:18:21 PM, 8:18:38 PM)**
    *   This file centralizes API calls related to admin functionalities, specifically for users and exam results.
    *   It defines functions for `apiListAdminUsers`, `apiCreateAdminUser`, `apiUpdateAdminUser`, `apiGetAdminUser`, `apiDeleteAdminUser`, and `apiGetExamResults`.
    *   All these functions include an `Authorization` header with a bearer token for authentication.
    *   Initially, it used a local `throwOnError` function to handle API response errors. In subsequent changes (8:18:21 PM and 8:18:38 PM), this error handling was refactored to use a shared `throwOnApiError` utility imported from `./errors`, indicating an effort to standardize error handling across the API layer. The content for the last two timestamps is identical, suggesting a quick save or no further functional change in that specific minute.

**Patterns and Recurring Elements:**

*   **Frontend Development Focus:** All changes are concentrated within the `frontend/src` directory, indicating active development on the user interface and its interaction with the backend.
*   **Authentication Dependency:** All frontend components that interact with the backend (like `ExamManagePage`, `UserManagement`) consistently use the `useAuth` hook to retrieve access tokens for authenticated API calls.
*   **API Service Layer:** There's a clear separation of concerns with dedicated `api` files (`admin.ts`, `auth.ts`, `exams.ts`, `problems.ts` are implied by imports in `ExamManagePage.tsx` and `AuthContext.tsx`) for interacting with the backend.
*   **Admin-Specific Features:** A strong emphasis on administrative capabilities for managing exams and users, suggesting an "Online Judge" system with interviewer/admin roles.
*   **Consistent Styling:** The `inputCls` variable is used uniformly across different React components for consistent input field styling.
*   **Error Handling Standardization:** The evolution of `api/admin.ts` shows a move towards centralized error handling with `throwOnApiError`, improving maintainability and consistency.
*   **State Management:** React components extensively use `useState` and `useEffect` hooks for managing local component state, side effects (like data fetching), and component lifecycle.
*   **CRUD Operations:** The API and UI components implement full Create, Read, Update, and Delete (CRUD) functionalities for exams and admin users.

## 10:06:58 PM
The development log shows significant activity across the frontend of an application, primarily focusing on authentication, exam management, and user management functionalities, all occurring on **May 28, 2026**.

**File-Specific Updates:**

*   **`frontend\src\contexts\AuthContext.tsx`** (Timestamp: 5/28/2026, 2:23:45 AM):
    *   This file introduces a comprehensive `AuthContext` for managing user authentication state.
    *   It defines the authentication state (`user`, `accessToken`, `loading`) and provides functions to `setAuth`, `clearAuth`, and `getAccessToken`.
    *   Key functionalities include:
        *   Recovering sessions from a `refresh_token` stored in `localStorage` upon component mount.
        *   Utilizing `apiRefresh` and `apiMe` for token refresh and user data retrieval.
        *   Helper functions (`getJwtExpSeconds`, `isAccessTokenFresh`) to validate JWT access tokens.
        *   It explicitly notes the trade-off of storing `refresh_token` in `localStorage` for simpler deployment over `httpOnly` cookies.

*   **`frontend\src\pages\ExamManagePage.tsx`** (Timestamp: 5/28/2026, 8:03:23 PM):
    *   This page is dedicated to managing exams, allowing for creation and editing.
    *   It integrates multiple API calls (`apiCreateExam`, `apiUpdateExam`, `apiDeleteExam`, `apiCreateAssignment`, `apiDeleteAssignment`, `apiListProblems`, `apiListAdminUsers`) to handle exam details, problems, and candidate assignments.
    *   State is managed for form inputs (title, description, start/end time, score visibility), loading, saving status, and error display.
    *   It includes mechanisms for selecting and assigning problems and candidates, complete with search and difficulty filters for problems.
    *   The page distinguishes between creating a new exam and editing an existing one based on the `examId` URL parameter.

*   **`frontend\src\pages\UserManagement.tsx`** (Timestamp: 5/28/2026, 8:05:11 PM):
    *   This component provides an administrative interface for managing users.
    *   It utilizes `apiListAdminUsers` for fetching a paginated list of users, and handles `apiDeleteAdminUser`, `apiCreateAdminUser`, `apiUpdateAdminUser`, and `apiDeactivateAdminUser` actions (though the implementation details for all these functions are not fully shown in the provided code snippet).
    *   Features include searching users by name/email, filtering by predefined roles ('admin', 'interviewer', 'problem_admin', 'candidate'), and pagination.
    *   The UI displays user details in a table and provides actions to edit, deactivate, or delete users, with safeguards preventing a user from deleting themselves.

*   **`frontend\src\api\admin.ts`** (Timestamp: 5/28/2026, 8:17:41 PM, 8:18:21 PM, 8:18:38 PM):
    *   This file defines the API client functions for administrative tasks related to users and exam results.
    *   It provides functions for:
        *   Listing admin users with pagination, role, and name filtering (`apiListAdminUsers`).
        *   Creating, updating, getting, and deleting individual admin users (`apiCreateAdminUser`, `apiUpdateAdminUser`, `apiGetAdminUser`, `apiDeleteAdminUser`).
        *   Fetching exam results (`apiGetExamResults`).
    *   A significant change occurred between 8:17:41 PM and 8:18:21 PM where the local `throwOnError` error handling function was refactored and replaced with an import `throwOnApiError` from a centralized `./errors` module. The subsequent timestamp (8:18:38 PM) shows no functional changes to the code.

**Patterns and Recurring Elements:**

*   **Authentication and Authorization**: All frontend components needing to interact with the backend consistently use the `useAuth` hook and its `getAccessToken()` function to retrieve and apply JWT bearer tokens for authorization headers in API requests. `localStorage` is a recurring pattern for storing `refresh_token`.
*   **API Client Structure**: The `src/api/admin.ts` file demonstrates a consistent pattern for API interactions: accepting a `token`, constructing URL parameters, setting `Authorization` and `Content-Type` headers, using `fetch`, and centralizing error handling logic.
*   **UI Development Practices**: Frontend pages (`ExamManagePage.tsx`, `UserManagement.tsx`) exhibit common React patterns:
    *   Using `useState` for managing component-specific data, form inputs, loading states, and error messages.
    *   Employing `useEffect` hooks for data fetching and side effects that depend on state or props changes.
    *   Consistent styling for input fields using `inputCls`.
    *   Clear separation of UI components and API interaction logic.
*   **Timestamp Clustering**: There's a concentrated period of development activity on May 28, 2026, primarily in the evening, indicating focused work on core application features after the initial authentication setup in the early morning. The rapid succession of changes to `api/admin.ts` suggests immediate refactoring or minor adjustments to newly introduced API client code.