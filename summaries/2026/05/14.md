# Activity Summary for 5/14/2026

## 3:04:56 PM
The file `c:\NTHU\Junior_I\Data Lab\RoyalRoad\README.md` was updated on `5/14/2026, 2:56:36 PM`.

This `README.md` details the "RoyalRoad Reader-Comment Generator," a pipeline designed to scrape chapters and reader comments from RoyalRoad, process them into training data, fine-tune a Qwen-based Large Language Model (LLM) using LoRA, and then use this model to generate authentic-sounding reader comments for any given chapter text.

The document outlines a clear, six-step pipeline:
1.  **`chapter_scraper.py`**: Scrapes top-rated fictions and their chapters into `chapters_dataset.jsonl`. It supports resuming crawls and includes polite delays between requests.
2.  **`comment_scraper.py`**: Retrieves reader comments for chapters, storing them in `comments_dataset.jsonl`. This script also supports automatic resumption.
3.  **`merge_comments.py`**: Processes raw reader comments by grouping them and using an Ollama-cloud model (`gemma4:31b-cloud`) to merge noisy comments into single, coherent, casual-toned entries, outputting `comments_merged.jsonl`. This step requires an `OLLAMA_API_KEY` to be set as an environment variable.
4.  **`prepare_sft_data.py`**: Combines chapters and the merged comments to create chat-format Supervised Fine-Tuning (SFT) examples, saved to `sft_data/train.jsonl`. It includes logic for truncating long chapters while preserving the beginning and end content.
5.  **`train_comment_sft.py`**: Fine-tunes the Qwen base model using QLoRA (4-bit base + bf16 LoRA) via TRL's `SFTTrainer`. It logs to Weights & Biases and supports resuming from checkpoints. It specifies model, epochs, batch size, LoRA parameters, and GPU requirements, noting the `HF_HOME` environment variable's importance.
6.  **`generate_comments.py`**: The final script that takes a chapter and generates reader comments using the fine-tuned model. It offers various flags to control generation mode (SFT or base model), input source (file, inline text, or index), metadata, and generation parameters like temperature and length.

**Key patterns and recurring elements** across the documentation include:
*   Extensive use of `.jsonl` format for dataset files, emphasizing line-delimited JSON.
*   Most data scraping and processing scripts (`chapter_scraper.py`, `comment_scraper.py`, `merge_comments.py`) are designed with resume functionality to handle interruptions and avoid redundant work.
*   Heavy reliance on the Hugging Face ecosystem for model management (base models like Qwen/Qwen3.5-9B) and sharing (LoRA adapters).
*   The project consistently leverages environment variables (e.g., `HF_HOME`, `OLLAMA_API_KEY`, `CUDA_VISIBLE_DEVICES`) for configuration and resource management.
*   The core task revolves around LLM fine-tuning, specifically Qwen models with LoRA/QLoRA for comment generation.

## 4:04:57 PM
The `README.md` file located at `c:\NTHU\Junior_I\Data Lab\RoyalRoad\README.md`, updated on `5/14/2026, 2:56:36 PM`, details the "RoyalRoad Reader-Comment Generator" project. This project is a pipeline designed to scrape chapters and reader comments from RoyalRoad, process them into training data, fine-tune a Qwen-based Large Language Model (LLM) using LoRA, and then use the fine-tuned model to generate authentic-sounding reader comments for chapter texts.

The `README` provides a comprehensive overview of the project, including:

*   **Project Overview and Pipeline Diagram:** A visual representation of the workflow, starting with `chapter_scraper.py` and `comment_scraper.py`, leading to data preparation scripts (`prepare_sft_data.py`, `merge_comments.py`), training (`train_comment_sft.py`), and finally comment generation (`generate_comments.py`).
*   **Quick Start (Inference Only):** Instructions for users who only want to run the `generate_comments.py` script with a pre-trained adapter. This involves cloning the repository, installing dependencies, optionally logging into Hugging Face, setting `HF_HOME` cache, downloading the LoRA adapter (e.g., `SamuelYo/royalroad-comment-sft`), and then executing the generation script with various parameters like `--chapter-file`, `--fiction`, `--num-comments`. It also explains `--mode sft` (default, uses adapter) and `--mode base` (uses only base model).
*   **Full Pipeline Breakdown:** Detailed explanations for each script in the pipeline:
    *   `chapter_scraper.py`: Scrapes top-rated fictions, outputs `chapters_dataset.jsonl`. Features resume capability and polite request delays.
    *   `comment_scraper.py`: Scrapes reader comments for chapters, outputs `comments_dataset.jsonl`. Resumes automatically.
    *   `merge_comments.py`: Distills noisy real reader comments by grouping them and using an Ollama-cloud model (`gemma4:31b-cloud`) to merge batches into single-voice, casual comments. Outputs `comments_merged.jsonl`. Requires `OLLAMA_API_KEY`.
    *   `prepare_sft_data.py`: Builds chat-format Supervised Fine-Tuning (SFT) examples by joining chapters and merged comments, outputting `sft_data/train.jsonl`. It truncates long chapters, focusing on the beginning and end.
    *   `train_comment_sft.py`: Fine-tunes the Qwen base model (default `Qwen/Qwen3.5-9B`) using QLoRA (4-bit base + bf16 LoRA) via TRL's `SFTTrainer`. It supports logging to Weights & Biases (W&B) and resuming from checkpoints. A single ~24 GB GPU is sufficient for the default setup.
    *   `generate_comments.py`: Described further with detailed flags for controlling generation (e.g., `--base-model`, `--adapter`, `--max-new-tokens`, `--temperature`).
*   **Sharing Checkpoints:** Instructions on how to upload the trained LoRA adapter to the Hugging Face Hub using `huggingface_hub`.

**Key Patterns and Recurring Elements:**

*   **Data Flow:** The pipeline consistently uses `.jsonl` files to store datasets (e.g., `chapters_dataset.jsonl`, `comments_dataset.jsonl`, `comments_merged.jsonl`, `sft_data/train.jsonl`).
*   **Model Specification:** `Qwen/Qwen3.5-9B` is consistently mentioned as the default base model for fine-tuning and generation.
*   **LoRA Adapter:** The concept of a LoRA adapter is central to the fine-tuning and inference process, with a dedicated path `./checkpoints/comment_sft/final` for the final output.
*   **Resumption and Efficiency:** Multiple scraping and merging scripts (`chapter_scraper.py`, `comment_scraper.py`, `merge_comments.py`) explicitly support resuming from previous runs to save time and resources.
*   **Command-Line Flexibility:** Most scripts offer a range of command-line arguments to customize behavior, such as `--extra-only` for scraping, `--max-chapter-chars` for data preparation and generation, and various `--lora-r`, `--epochs`, `--lr` for training.
*   **Hugging Face Integration:** Hugging Face is used extensively for base model access, adapter sharing, and potentially for caching.
*   **LLM for Data Preprocessing:** An external LLM (Ollama-cloud `gemma4:31b-cloud`) is employed for the specific task of merging comments, indicating a multi-model approach.
*   **Environment Variables:** The use of environment variables like `OLLAMA_API_KEY`, `HF_HOME`, and `CUDA_VISIBLE_DEVICES` for configuration is noted.