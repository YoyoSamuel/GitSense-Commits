# Activity Summary for 5/30/2026

## 12:07:07 AM
The code changes primarily focus on implementing authentication, exam management, and user management features in a web application's frontend.

**File-Specific Updates:**

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\contexts\AuthContext.tsx`** (Timestamp: 5/28/2026, 2:23:45 AM):
    *   Introduces an `AuthContext` and `AuthProvider` for global authentication state management.
    *   Handles user login status, access tokens, and a loading state for initial session recovery.
    *   Includes logic for refreshing access tokens using `localStorage` stored refresh tokens and verifying JWT expiration.
    *   Provides `setAuth`, `clearAuth`, and `getAccessToken` functions for components to interact with authentication.
*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ExamManagePage.tsx`** (Timestamp: 5/28/2026, 8:03:23 PM):
    *   Implements a page for creating, editing, and deleting exams.
    *   Manages exam details such as title, description, start/end times, and whether to show scores.
    *   Allows associating problems and candidates with an exam, with features for searching, filtering by difficulty, and selecting/deselecting items.
    *   Handles API calls for exam CRUD operations and assignment management.
*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\UserManagement.tsx`** (Timestamp: 5/28/2026, 8:05:11 PM):
    *   Provides an administrative interface for managing users.
    *   Displays a paginated list of users, allowing search by name/email and filtering by role (admin, interviewer, problem\_admin, candidate).
    *   Includes functionality to navigate to create or edit user profiles, and to deactivate or delete users.
    *   Shows user details like name, email, role, status (active/inactive), and creation date.
*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\api\admin.ts`** (Timestamp: 5/28/2026, 8:17:41 PM; 5/28/2026, 8:18:21 PM; 5/28/2026, 8:18:38 PM):
    *   Defines a set of API client functions for interacting with backend admin endpoints related to users and exams.
    *   Includes functions for listing, creating, updating, getting, and deleting admin users (`apiListAdminUsers`, `apiCreateAdminUser`, `apiUpdateAdminUser`, `apiGetAdminUser`, `apiDeleteAdminUser`).
    *   Also provides an API to get exam results (`apiGetExamResults`).
    *   A significant change between 8:17:41 PM and 8:18:21 PM was refactoring the error handling mechanism: the inline `throwOnError` helper function was replaced by an import of `throwOnApiError` from a separate `./errors` file, indicating a move towards centralized API error handling. The last change at 8:18:38 PM was functionally identical, likely a save without further modification.

**Timestamps of Significant Changes:**

*   **5/28/2026, 2:23:45 AM:** Core authentication context setup, establishing session management and token handling.
*   **5/28/2026, 8:03:23 PM to 8:05:11 PM:** Rapid development of administrative frontend pages for `ExamManagePage` and `UserManagement`, indicating focused work on admin functionalities.
*   **5/28/2026, 8:17:41 PM to 8:18:21 PM:** Immediate updates to the `api/admin.ts` file, specifically refactoring error handling, suggesting a quick improvement or standardization of API client practices after implementing the admin pages.

**Patterns or Recurring Elements:**

*   **Token-Based Authentication:** All frontend interactions with the API (especially `admin.ts`) consistently use an `Authorization: Bearer ${token}` header, managed through the `AuthContext`.
*   **Centralized API Calls:** API-specific functions are grouped in dedicated files (e.g., `api/admin.ts`), promoting modularity and reusability.
*   **Frontend State Management with Hooks:** React's `useState` and `useEffect` hooks are widely used across page components (`ExamManagePage`, `UserManagement`) for managing component-specific data, loading states, and side effects.
*   **Error Handling Strategy:** A consistent pattern of `try...catch` blocks is used in page components to handle API errors, coupled with a `throwOnApiError` utility in the API client layer for standardized response error processing.
*   **UI Consistency:** The `inputCls` variable is defined and reused across multiple pages for styling input elements, suggesting a common design language or utility-first CSS approach.
*   **CRUD Operations:** Both `ExamManagePage` and `UserManagement` implement standard Create, Read, Update, and Delete (CRUD) operations for their respective entities (Exams, Users).
*   **Rapid Iteration:** The close proximity of timestamps in the evening of May 28th, 2026, suggests a period of active development and minor refactoring.

## 1:06:59 AM
The provided log details several code changes primarily focused on the frontend of an Online Judge (OJ) system, specifically concerning authentication, exam management, and user administration.

### File-Specific Updates:

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\contexts\AuthContext.tsx`**
    *   **Timestamp**: 5/28/2026, 2:23:45 AM
    *   **Summary**: This file defines the `AuthContext` for global authentication state management. It handles user login status, access token retrieval, and refresh token management (stored in `localStorage`). Key features include:
        *   **Session Recovery**: On component mount, it attempts to recover the user session using a stored refresh token via `apiRefresh` and `apiMe`.
        *   **Token Freshness**: Includes a `getJwtExpSeconds` utility and `isAccessTokenFresh` function to determine if the current access token needs refreshing, incorporating a `TOKEN_REFRESH_SKEW_SECONDS`.
        *   **Authentication Operations**: Provides `setAuth` to establish a new authenticated session, `clearAuth` to log out (including calling `apiLogout`), and `getAccessToken` to reliably provide a fresh access token, refreshing it if necessary.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ExamManagePage.tsx`**
    *   **Timestamp**: 5/28/2026, 8:03:23 PM
    *   **Summary**: This new page facilitates the creation and management of exams. It supports:
        *   **Exam Creation/Editing**: Allows users to create new exams or edit existing ones, defining details like title, description, start/end times, and whether to show scores.
        *   **Problem & Candidate Management**: Enables associating problems and candidates with an exam. It includes UI for searching and filtering problems by difficulty, and selecting multiple problems/candidates.
        *   **API Integration**: Uses various API calls (`apiCreateExam`, `apiUpdateExam`, `apiDeleteExam`, `apiListProblems`, `apiListAdminUsers`, `apiCreateAssignment`, `apiDeleteAssignment`) to interact with the backend.
        *   **UI/UX**: Features loading/saving states, error display, and confirmation prompts for destructive actions (e.g., deleting an exam). Date/time inputs are formatted using `toLocalDatetime`.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\UserManagement.tsx`**
    *   **Timestamp**: 5/28/2026, 8:05:11 PM
    *   **Summary**: This page provides an administrative interface for managing users. It allows:
        *   **User Listing & Filtering**: Displays a paginated list of users, with options to search by name/email and filter by role (`admin`, `interviewer`, `problem_admin`, `candidate`).
        *   **User Actions**: Implements functionality to update user roles (`handleSave` using `apiUpdateAdminUser`), deactivate users (`handleDeactivate` using `apiDeactivateAdminUser`), and delete users. It prevents a user from deleting their own active account.
        *   **API Integration**: Relies on `apiListAdminUsers` for fetching user data and `apiDeleteAdminUser` for deletion.
        *   **UI/UX**: Features loading states, error display, and formatted creation dates (`formatDate`).

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\api\admin.ts`**
    *   **Timestamp 1**: 5/28/2026, 8:17:41 PM
    *   **Timestamp 2**: 5/28/2026, 8:18:21 PM
    *   **Timestamp 3**: 5/28/2026, 8:18:38 PM
    *   **Summary**: This file defines a set of API client functions for administrative tasks.
        *   **Core Functions**: Provides methods to `list`, `create`, `update`, `get`, and `delete` admin users (`apiListAdminUsers`, `apiCreateAdminUser`, `apiUpdateAdminUser`, `apiGetAdminUser`, `apiDeleteAdminUser`).
        *   **Exam Results**: Includes `apiGetExamResults` to fetch results for a specific exam.
        *   **Authentication**: All functions require an authentication token, which is passed in the `Authorization` header.
        *   **Error Handling Refinement**: A significant change occurred at 8:18:21 PM, where the internal `throwOnError` helper was refactored to utilize a `throwOnApiError` function imported from a new `./errors` module, centralizing API error processing. The subsequent entry at 8:18:38 PM shows no functional difference, likely a re-save.
        *   **Query Parameters**: `apiListAdminUsers` supports query parameters for pagination (`page`, `page_size`), `role`, and `name`.

### Patterns and Recurring Elements:

*   **Authentication Dependency**: A strong pattern is the reliance on an authentication token (`token` or `freshToken`) for nearly all API interactions, often retrieved via `getAccessToken()`. This token is consistently included in the `Authorization: Bearer ${token}` header for every `fetch` request.
*   **Centralized Error Handling**: The `frontend/src/api/admin.ts` file demonstrates a pattern of centralizing API error handling, initially with a local `throwOnError` and later refactored to `throwOnApiError` from a dedicated `errors` module. Frontend components typically catch these errors and update a local `error` state for user feedback.
*   **Loading and Saving States**: Frontend components (`ExamManagePage.tsx`, `UserManagement.tsx`) frequently use `loading` and `saving` boolean states to manage UI feedback (e.g., displaying "Loading…" messages, disabling buttons) during asynchronous operations.
*   **Consistent UI Styling**: A recurring `inputCls` variable is defined and used across multiple pages (`ExamManagePage.tsx`, `UserManagement.tsx`) for consistent styling of form input elements, suggesting a component-level styling pattern.
*   **Data Fetching with `useEffect`**: `useEffect` hooks are widely used in pages to fetch initial data or react to token changes, often setting `loading` states.
*   **State Management with `useState` and `useCallback`**: `useState` is heavily utilized for managing component-specific data and form inputs, while `useCallback` is employed for memoizing functions (e.g., in `AuthContext`) to optimize performance.
*   **API Structure**: API endpoint paths generally follow `/api/v1/{resource}/{id}` patterns, and most `fetch` calls are `async/await` wrapped, handling JSON serialization/deserialization.

## 2:06:54 AM
The `AuthContext.tsx` file, last modified on **May 28, 2026, at 2:23:45 AM**, defines the application's authentication context. It manages user state, access tokens, and refresh tokens. Key functionalities include initial session recovery from `localStorage` using refresh tokens, setting and clearing authentication data, and a `getAccessToken` method that handles refreshing the access token if it's expired. It also includes utility functions like `getJwtExpSeconds` and `isAccessTokenFresh` for JWT token validation.

The `ExamManagePage.tsx` file, updated on **May 28, 2026, at 8:03:23 PM**, implements a comprehensive page for managing exams. This includes creating new exams and editing existing ones. It allows for setting exam details such as title, description, start/end times, and whether to show scores to candidates. The page also provides interfaces for selecting and managing problems and candidates associated with an exam, featuring search and filter options for problems. Functions are present for saving, deleting exams, and adding/removing assignments (linking problems to candidates for an exam).

The `UserManagement.tsx` file, changed on **May 28, 2026, at 8:05:11 PM**, provides an administrative interface for managing users. It lists users with details like name, email, role, status (active/inactive), and creation date. The page supports pagination, searching users by name or email, and filtering by predefined roles (admin, interviewer, problem\_admin, candidate). Users can be edited or deleted, and there's a specific `handleDeactivate` function to change a user's active status.

The `api\admin.ts` file saw several rapid updates on **May 28, 2026, between 8:17:41 PM and 8:18:38 PM**. This file defines a set of asynchronous functions for interacting with the `/api/v1/admin` endpoints, covering administrative operations such as listing, creating, updating, getting, and deleting admin users, as well as fetching exam results. A significant refactoring occurred during this period: initially, the error handling logic was directly implemented in a `throwOnError` function, but it was quickly refactored to import and use a centralized `throwOnApiError` utility from `./errors`, indicating an effort to standardize API error handling across the frontend.

**Recurring elements and patterns** across these changes include:
*   **Token-based authentication:** All API calls in `api\admin.ts` and token management in `AuthContext.tsx` rely on `Bearer` tokens for authorization.
*   **Centralized API interaction:** Frontend pages utilize dedicated API helper functions (e.g., `apiListAdminUsers`, `apiCreateExam`) for data fetching and manipulation.
*   **Consistent UI styling:** Custom CSS class names like `inputCls`, `bg-oj-surface2`, `border-oj-border`, and `focus:ring-oj-accent` are frequently used, suggesting a common design system for input fields and UI elements.
*   **Date formatting utilities:** Helper functions like `toLocalDatetime` and `formatDate` are employed for consistent date and time display.
*   **CRUD operations:** Both exam and user management pages implement full Create, Read, Update, and Delete (CRUD) functionalities for their respective data models.

## 3:06:53 AM
The log details recent developments across the frontend of an Online Judge (OJ) web application, primarily focusing on authentication, exam management, and user administration.

**File-Specific Updates:**

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\contexts\AuthContext.tsx`** (Timestamp: 5/28/2026, 2:23:45 AM)
    This file establishes the core authentication mechanism for the frontend. It defines an `AuthContext` and `AuthProvider` to manage user authentication state, including `user`, `accessToken`, and `loading` status. Key functionalities include:
    *   Handling JWT (JSON Web Token) access tokens, with logic to extract expiration times.
    *   Implementing a token refresh mechanism using a stored `refresh_token` in `localStorage`, which is retrieved upon component mount (`useEffect`).
    *   Providing `setAuth`, `clearAuth`, and `getAccessToken` methods for managing the authentication state and ensuring fresh tokens.
    *   It integrates with `apiLogout`, `apiMe`, and `apiRefresh` for backend authentication interactions.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ExamManagePage.tsx`** (Timestamp: 5/28/2026, 8:03:23 PM)
    This significant update introduces a comprehensive page for managing exams. The component allows administrators to:
    *   Create new exams or edit existing ones, handling exam details such as title, description, start/end times, and whether to show scores to candidates.
    *   Associate problems with an exam, with features for searching problems by title and filtering by difficulty.
    *   Assign candidates to an exam, with capabilities to search for candidates.
    *   Perform CRUD (Create, Read, Update, Delete) operations for exams and assignments (`handleSave`, `handleDelete`, `handleAddAssignment`, `handleRemoveAssignment`).
    *   It uses `useParams` to determine if it's a new or existing exam and fetches relevant data (`apiGetExam`, `apiListAssignments`, `apiListProblems`, `apiListAdminUsers`).
    *   Includes UI elements for input forms, checkboxes, search bars, and selection lists.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\UserManagement.tsx`** (Timestamp: 5/28/2026, 8:05:11 PM)
    This component provides an administrative interface for managing users. It features:
    *   Listing admin users with pagination.
    *   Search functionality by user name or email.
    *   Filtering users by their roles (admin, interviewer, problem_admin, candidate).
    *   Actions to edit user details, create new users (navigates to a new user page), and delete/deactivate existing users (`handleDelete`, `handleDeactivate`).
    *   Displays user information in a table, including name, email, role, status (active/inactive), and creation date.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\api\admin.ts`** (Multiple updates: 5/28/2026, 8:17:41 PM; 5/28/2026, 8:18:21 PM; 5/28/2026, 8:18:38 PM)
    This file defines the API client functions for interacting with administrative backend endpoints. It exposes functions for:
    *   `apiListAdminUsers`: Fetching a list of admin users with support for pagination, role filtering, and name search.
    *   `apiCreateAdminUser`, `apiUpdateAdminUser`, `apiGetAdminUser`, `apiDeleteAdminUser`: Standard CRUD operations for admin users.
    *   `apiGetExamResults`: Fetching exam results for a specific exam.
    *   A significant micro-change occurred at 8:18:21 PM where the internal `throwOnError` helper was refactored to delegate error handling to a more generalized `throwOnApiError` function from a separate `errors` module, indicating a move towards centralized API error handling. The subsequent timestamp shows no functional changes.

**Patterns and Recurring Elements:**

*   **Authentication Token Usage:** A consistent pattern of retrieving an authentication token (`getAccessToken().then(setToken)`) and using it in `Authorization: Bearer ${token}` headers for all protected API calls.
*   **API Interaction Abstraction:** All frontend API calls are routed through dedicated API modules (e.g., `api/admin.ts`), abstracting raw `fetch` requests.
*   **Centralized Error Handling:** The `throwOnError` / `throwOnApiError` pattern is used across API functions to standardize how HTTP errors are caught and re-thrown as JavaScript errors.
*   **State Management with Hooks:** Heavy reliance on React `useState` and `useEffect` hooks for managing component-specific data, loading states, error messages, form inputs, and fetching data on component mount or dependency changes.
*   **UI Consistency:** The `inputCls` variable is defined and reused to apply consistent styling to input fields across different management pages, indicating a design system approach.
*   **Admin-Focused Development:** A clear pattern of adding and enhancing administrative capabilities, particularly around user and exam management.
*   **Datetime Utilities:** Custom utility functions like `toLocalDatetime` and `formatDate` are implemented to format timestamps for user display and backend submission, showcasing specific date/time handling needs.

## 4:06:55 AM
The `AuthContext.tsx` file, updated on 5/28/2026 at 2:23:45 AM, introduces a core authentication context (`AuthContext` and `AuthProvider`) for the frontend. This includes state management for user information and access tokens, functions to set, clear, and refresh authentication credentials, and logic to recover sessions from stored refresh tokens. It also features utilities for checking JWT expiration and token freshness, utilizing `localStorage` for refresh token persistence as an intentional trade-off.

The `ExamManagePage.tsx` file, updated on 5/28/2026 at 8:03:23 PM, is a comprehensive component for managing exams. It supports creating new exams and editing existing ones, including updating basic exam details (title, description, start/end times, score visibility), and managing associated problems and candidates. The page fetches all available problems and admin users (identified as candidates) to allow for selection and assignment to an exam. It implements CRUD operations for exams and assignments via various API calls (`apiCreateAssignment`, `apiDeleteAssignment`, `apiCreateExam`, `apiDeleteExam`, `apiGetExam`, `apiListAssignments`, `apiListProblems`, `apiListAdminUsers`), handles form data, loading states, and error messages, and includes UI elements for filtering problems by difficulty and searching.

The `UserManagement.tsx` file, updated on 5/28/2026 at 8:05:11 PM, provides a user interface for managing admin users within the system. It lists users with details like name, email, role, status, and creation date, supporting pagination. Administrators can search for users by name/email and filter by predefined roles (`admin`, `interviewer`, `problem_admin`, `candidate`). The page integrates API calls for listing, creating, updating, deactivating, and deleting admin users (`apiListAdminUsers`, `apiCreateAdminUser`, `apiUpdateAdminUser`, `apiDeactivateAdminUser`, `apiDeleteAdminUser`). It also includes navigation to user creation/edit pages and confirmation prompts for deletion/deactivation.

The `api\admin.ts` file, with multiple updates on 5/28/2026 between 8:17:41 PM and 8:18:38 PM, defines the frontend API client for administrative operations. This file provides functions for `apiListAdminUsers`, `apiCreateAdminUser`, `apiUpdateAdminUser`, `apiGetAdminUser`, `apiDeleteAdminUser`, and `apiGetExamResults`. All these functions construct API requests, send authorization tokens, and parse responses. A key pattern observed is the evolution of error handling: initially, it included a local `throwOnError` function to process HTTP errors; subsequently, this was refactored to import and use a centralized `throwOnApiError` function from `./errors`, indicating an effort to standardize API error handling across the application.

**Patterns and Recurring Elements:**

*   **Timestamps**: The changes are clustered, with foundational authentication logic updated in the early morning of May 28, 2026, followed by a concentrated period of development on administrative features (exam and user management, and their corresponding API integrations) in the late evening of the same day.
*   **Frontend-Focused Development**: All changes (excluding the `.env` file) are within the `frontend/src` directory, indicating active development on the client-side application.
*   **API Integration**: Components heavily rely on dedicated API functions for data fetching and manipulation (e.g., `apiListProblems`, `apiListAdminUsers`, `apiCreateExam`).
*   **State Management with Hooks**: React hooks (`useState`, `useEffect`, `useCallback`) are extensively used for managing component state, handling side effects (like data fetching on mount or token changes), and memoizing functions.
*   **Consistent UI Styling**: The use of `inputCls` and similar utility functions (e.g., `toLocalDatetime`, `formatDate`) suggests a focus on maintaining a consistent look and feel across different forms and display elements.
*   **Error Handling**: A clear pattern emerges in API interactions where dedicated error-checking functions (`throwOnError`, `throwOnApiError`) are consistently called after fetch requests to standardize error propagation.
*   **Role-Based Functionality**: The `UserManagement` page and related API calls explicitly handle different user roles, indicating a system with defined access levels.

## 5:06:56 AM
The provided log details recent development focusing on frontend features related to authentication, exam management, and user administration.

**File-specific Updates:**

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\contexts\AuthContext.tsx`** (Timestamp: 5/28/2026, 2:23:45 AM)
    This file establishes the core authentication context for the frontend. It defines `AuthContext` and `AuthProvider` to manage user sessions, including `user` data, `accessToken`, and `loading` state. Key functionalities include:
    *   `setAuth`: Stores user credentials and a refresh token (in `localStorage`).
    *   `clearAuth`: Removes authentication data and logs out the user.
    *   `getAccessToken`: Retrieves a fresh access token, potentially using the stored refresh token.
    *   Helper functions `getJwtExpSeconds` and `isAccessTokenFresh` are used to check JWT expiration, accounting for a `TOKEN_REFRESH_SKEW_SECONDS`.
    *   An `useEffect` hook attempts to recover the session from `localStorage` on component mount, refreshing the token if available. The use of `localStorage` for the refresh token is explicitly noted as a trade-off for single-machine deployments.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ExamManagePage.tsx`** (Timestamp: 5/28/2026, 8:03:23 PM)
    This React component is responsible for creating, editing, and deleting exams. It features a comprehensive UI for managing exam details, problems, and candidates:
    *   It fetches existing exam data, associated assignments, available problems, and candidate users on load, distinguishing between new exam creation and editing an existing one.
    *   A form manages exam properties like `title`, `description`, `start_time`, `end_time`, and `show_score`.
    *   It allows users to select problems and candidates for the exam, with search and difficulty filters for problems.
    *   CRUD operations for exams and assignments are handled by `handleSave`, `handleDelete`, `handleAddAssignment`, and `handleRemoveAssignment` functions, interacting with various API endpoints.
    *   Loading and error states are managed to provide user feedback.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\UserManagement.tsx`** (Timestamp: 5/28/2026, 8:05:11 PM)
    This component provides an administrative interface for managing users. It displays a paginated list of admin users and offers filtering and search capabilities:
    *   It retrieves a list of users, including their `name`, `email`, `role`, `status` (active/inactive), and `created_at` date.
    *   Users can be filtered by `role` (admin, interviewer, problem_admin, candidate) and searched by `name` or `email`.
    *   Actions are provided to navigate to edit/create user pages, deactivate users, and delete users.
    *   Loading and error states are displayed during API interactions.
    *   It utilizes the `useAuth` hook to obtain the necessary access token for API calls.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\api\admin.ts`** (Multiple entries: 5/28/2026, 8:17:41 PM; 8:18:21 PM; 8:18:38 PM)
    This file defines the API client functions for administrative operations related to users and exam results.
    *   It includes functions for listing, creating, updating, getting, and deleting admin users (`apiListAdminUsers`, `apiCreateAdminUser`, `apiUpdateAdminUser`, `apiGetAdminUser`, `apiDeleteAdminUser`).
    *   It also contains a function to retrieve exam results (`apiGetExamResults`).
    *   All functions send a `Bearer` token in the `Authorization` header.
    *   A significant change occurred between 8:17:41 PM and 8:18:21 PM: the custom `throwOnError` helper was refactored to use a centralized `throwOnApiError` utility imported from a new `./errors` module. The last entry at 8:18:38 PM shows no functional change.

**Patterns and Recurring Elements:**

*   **Authentication-driven Interactions:** All frontend components and API calls depend on an access token managed by the `AuthContext` to authenticate requests.
*   **CRUD Operations:** There's a strong pattern of implementing Create, Read, Update, and Delete (CRUD) operations across both `ExamManagePage` and `UserManagement` through dedicated API functions.
*   **State Management for UI Feedback:** Frontend components consistently use `useState` hooks to manage `loading` and `error` states, providing clear feedback to the user during asynchronous operations.
*   **Modular API Design:** API logic is encapsulated in `src/api` files, promoting code organization and reusability.
*   **Consistent Styling:** The `inputCls` variable is used repeatedly to apply a uniform style to input fields across different pages.
*   **Error Handling Refinement:** The refactoring of error handling in `src/api/admin.ts` to use a shared `throwOnApiError` indicates an ongoing effort to standardize error management across the application.
*   **Timestamp Trend:** The timestamps indicate a concentrated period of development on May 28, 2026, particularly on frontend features for exam and user administration, following an initial setup of the authentication context.

## 7:06:57 AM
The provided log details several code changes within a frontend application, focusing on authentication, exam management, and user administration. The `.env` file content has been excluded as per instructions.

**File-Specific Updates:**

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\contexts\AuthContext.tsx`** (Timestamp: 5/28/2026, 2:23:45 AM)
    This file defines the `AuthContext` for managing user authentication state across the application. It includes:
    *   `AuthState` interface (`user`, `accessToken`, `loading`).
    *   `AuthContextValue` interface, adding `setAuth`, `clearAuth`, and `getAccessToken` methods.
    *   Utility functions `getJwtExpSeconds` and `isAccessTokenFresh` to check JWT expiration, accounting for a `TOKEN_REFRESH_SKEW_SECONDS`.
    *   An `AuthProvider` component that handles initial session recovery from `localStorage` using a `refresh_token`.
    *   `setAuth` to store user details and tokens, `clearAuth` to log out (also removing the `refresh_token` and calling `apiLogout`).
    *   `getAccessToken` to provide a fresh access token, attempting to refresh it if expired or missing, storing the `refresh_token` in `localStorage` as an explicit trade-off for simplicity in a single-machine deployment.
    *   A `useAuthContext` hook for consuming the context.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ExamManagePage.tsx`** (Timestamp: 5/28/2026, 8:03:23 PM)
    This React page is dedicated to creating and editing exams. Key features include:
    *   Dynamic routing based on `examId` to differentiate between new exam creation and existing exam management.
    *   Fetching exam details, problems, and candidate users using various API calls (`apiGetExam`, `apiListProblems`, `apiListAdminUsers`, `apiListAssignments`).
    *   Form management for exam attributes like title, description, start/end times, and whether to show scores.
    *   Functionality to save (`handleSave`), delete (`handleDelete`), and manage assignments (add/remove problems and candidates, `handleAddAssignment`, `handleRemoveAssignment`).
    *   UI elements for searching and filtering problems by title and difficulty, and selecting candidates.
    *   Date utility `toLocalDatetime` for display and form handling.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\UserManagement.tsx`** (Timestamp: 5/28/2026, 8:05:11 PM)
    This component provides an administrative interface for managing users. It features:
    *   Listing admin users with pagination, search by name/email, and filtering by role (`apiListAdminUsers`).
    *   Defined `ROLES` for user types: `['admin', 'interviewer', 'problem_admin', 'candidate']`.
    *   Functionality to create new users, update existing user roles/names, and deactivate users (`apiCreateAdminUser`, `apiUpdateAdminUser`, `apiDeactivateAdminUser`).
    *   Displays user information including name, email, role, active status, and creation date.
    *   `formatDate` utility for displaying user creation dates.
    *   Navigation to "New user" and "Edit" user pages.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\api\admin.ts`** (Significant changes at 5/28/2026, 8:17:41 PM, 8:18:21 PM, 8:18:38 PM)
    This file centralizes API calls related to admin functionalities.
    *   **Initial version (8:17:41 PM):** Implemented various API endpoints for `admin/users` and `admin/exams/{examId}/results`:
        *   `apiListAdminUsers`, `apiCreateAdminUser`, `apiUpdateAdminUser`, `apiGetAdminUser`, `apiDeleteAdminUser`.
        *   `apiGetExamResults`.
        *   Introduced a local `throwOnError` helper to abstract error handling for HTTP responses.
    *   **Refinement (8:18:21 PM):** The `throwOnError` function was refactored to use a shared `throwOnApiError` helper imported from `./errors`, improving consistency in error handling across API modules.
    *   **Minor change (8:18:38 PM):** No functional code changes were observed; this likely represents a re-save or minor non-functional update.

**Patterns and Recurring Elements:**

*   **Authentication & Authorization:** All API interactions in the frontend rely on an `accessToken` obtained via the `AuthContext`'s `getAccessToken` method and passed as a `Bearer` token in the `Authorization` header.
*   **API Interaction Structure:** A common pattern emerges for API calls: define a `BASE` URL, construct queries using `URLSearchParams`, use `fetch`, include `Authorization` and `Content-Type` headers, and process responses using a shared error-handling utility (`throwOnError`/`throwOnApiError`).
*   **React State Management:** `useState` and `useEffect` hooks are extensively used in page components (`ExamManagePage`, `UserManagement`) for managing loading states, error messages, form data, and fetched data.
*   **Asynchronous Operations:** Extensive use of `async`/`await` patterns for handling API requests and updates, often with `try...catch...finally` blocks to manage `loading` and `error` states.
*   **UI Design Consistency:** Components frequently use a set of Tailwind CSS classes (e.g., `inputCls`, `text-oj-fg`, `bg-oj-accent`, `border-oj-border`) indicating a cohesive design system and styling approach across the application.
*   **Data Transformation:** Utility functions like `toLocalDatetime` and `formatDate` are used to convert API date strings into user-friendly local date/time formats.

## 8:06:55 AM
This log details several significant updates to the frontend of a web application, primarily focusing on authentication and administrative features, all occurring on May 28, 2026.

**File-Specific Updates:**

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\contexts\AuthContext.tsx`** (Timestamp: 5/28/2026, 2:23:45 AM)
    *   This file defines the `AuthContext` and `AuthProvider` for managing user authentication state.
    *   It includes functionality for handling user login, logout, and token refreshing, leveraging `localStorage` for refresh tokens.
    *   Key helper functions are introduced for JWT expiration parsing (`getJwtExpSeconds`) and access token freshness validation (`isAccessTokenFresh`).
    *   The context provides methods to `setAuth`, `clearAuth`, and `getAccessToken`, ensuring a fresh access token is always available, with `useEffect` handling initial session recovery and `useCallback` for memoizing authentication functions.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ExamManagePage.tsx`** (Timestamp: 5/28/2026, 8:03:23 PM)
    *   Introduces a comprehensive React component for managing exams. Users can create new exams, edit existing ones, and delete them.
    *   It allows for setting exam details like title, description, start/end times, and whether to show scores to candidates.
    *   Crucially, this page enables the selection and de-selection of problems and candidates for an exam, with search and filtering capabilities (by problem difficulty, candidate roles).
    *   It integrates with multiple API endpoints for exams, problems, and admin users (`apiCreateExam`, `apiUpdateExam`, `apiDeleteExam`, `apiListProblems`, `apiListAdminUsers`, `apiCreateAssignment`, `apiDeleteAssignment`).
    *   Includes robust state management for form data, loading/saving indicators, and error handling.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\UserManagement.tsx`** (Timestamp: 5/28/2026, 8:05:11 PM)
    *   Adds a React component for administrative user management.
    *   It displays a paginated list of users with details like name, email, role, status (active/inactive), and creation date.
    *   Features include searching users by name/email and filtering by role.
    *   Provides actions to edit, delete, or deactivate users, interacting with `apiListAdminUsers`, `apiDeleteAdminUser`, and implying similar update/create APIs.
    *   The component handles loading states, pagination, and error display for user operations.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\api\admin.ts`** (Timestamps: 5/28/2026, 8:17:41 PM; 5/28/2026, 8:18:21 PM; 5/28/2026, 8:18:38 PM)
    *   This file defines the API client functions for interacting with admin-related endpoints, specifically for user management and exam results.
    *   Functions include `apiListAdminUsers`, `apiCreateAdminUser`, `apiUpdateAdminUser`, `apiGetAdminUser`, `apiDeleteAdminUser`, and `apiGetExamResults`.
    *   A significant pattern across the three rapid updates to this file is a **refactoring of error handling**. Initially, it used a custom `throwOnError` function. In subsequent commits, this was replaced by importing a centralized `throwOnApiError` from a separate `./errors` module, indicating an improvement in API error management consistency.

**Patterns and Recurring Elements:**

*   **Authentication Dependency:** All significant frontend components developed in this log (`AuthContext`, `ExamManagePage`, `UserManagement`) explicitly rely on an authentication token, retrieved via `getAccessToken`, to make API requests, reinforcing a secure, token-based authentication system.
*   **API-Centric Development:** The changes heavily involve interaction with a backend API (`/api/v1` base path), with numerous helper functions created to encapsulate API calls.
*   **React Hooks for State Management:** `useState` and `useEffect` are consistently used across components for managing local state, data fetching lifecycles, and UI feedback (loading, saving, errors).
*   **Consistent Error Handling:** There's a clear pattern of implementing `try...catch` blocks around API calls to handle potential errors gracefully and display feedback to the user. The evolution of `api/admin.ts` further shows a move towards a standardized global API error handling mechanism.
*   **Admin Features Focus:** The updates to `ExamManagePage` and `UserManagement` highlight a strong emphasis on building out administrative functionalities for managing core application entities like exams and users.
*   **Rapid Iteration:** The close timestamps for `api/admin.ts` (within a minute) suggest quick, focused changes, likely for refactoring or minor adjustments rather than major feature additions, pointing to an iterative development process.