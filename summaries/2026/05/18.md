# Activity Summary for 5/18/2026

## 5:10:39 PM
The provided log details a series of code changes, all occurring on **5/18/2026** within a short timeframe, indicating focused development on a conservative concurrency control mechanism within the VanillaDB project.

**File-Specific Updates:**

*   **`c:\NTHU\Junior_II\DBSM2\hw5\hw5\core-patch\src\main\java\org\vanilladb\core\storage\tx\concurrency\conservative\PrimaryKey.java`**
    *   **2:34:38 PM**: Initial implementation of a `PrimaryKey` class, storing a `tableName` and a `Map` of key-value entries. It included `equals`, `hashCode`, and `toString` methods, with `getKeyEntryMap()` returning an unmodifiable map.
    *   **2:42:53 PM**: The `getKeyEntryMap()` method (which returned an unmodifiable map), and the `toString()` method were removed. The `hashCode()` implementation was also revised.
    *   **2:46:43 PM - 2:55:55 PM**: Minor textual changes, primarily commenting out the package declaration and adding "new implement" comments, with no functional code changes during these timestamps.

*   **`c:\NTHU\Junior_II\DBSM2\hw5\hw5\core-patch\src\main\java\org\vanilladb\core\storage\tx\concurrency\ConservativeConcurrencyMgr.java`**
    *   **2:54:35 PM**: The class `ConservativeConcurrencyMgr` was initially committed as a skeleton, extending `ConcurrencyMgr` with all its abstract methods containing `// TODO` or `// Auto-generated method stub` comments.
    *   **2:56:49 PM**: Underwent a significant overhaul. It was fully implemented to handle conservative concurrency.
        *   It introduced a constructor `ConservativeConcurrencyMgr(long txNumber)` and a `LOCK_PREPARE_MUTEX`.
        *   A new public method `prepareSp(Set<PrimaryKey> readSet, Set<PrimaryKey> writeSet)` was added to pre-acquire `xLock` for write keys and `sLock` for read-only keys using `lockTbl`.
        *   All previously stubbed methods (`onTxCommit`, `onTxRollback`, `modifyFile`, `readFile`, `insertBlock`, `modifyBlock`, `readBlock`, `modifyIndex`, `readIndex`, `readLeafBlock`, `modifyLeafBlock`) were fleshed out with specific lock acquisition/release logic, signifying a complete implementation of conservative locking at different granularity levels. `modifyRecord` and `readRecord` were explicitly noted as having logical locks acquired before SQL execution.
    *   **2:57:02 PM**: A "new implement" comment was added.

*   **`c:\NTHU\Junior_II\DBSM2\hw5\hw5\core-patch\src\main\java\org\vanilladb\core\sql\storedprocedure\StoredProcedureParamHelper.java`**
    *   **3:01:11 PM**: The abstract `StoredProcedureParamHelper` class was introduced with basic methods.
    *   **3:01:17 PM - 3:01:49 PM**: Imports for `java.util.Collections`, `java.util.Set`, and `org.vanilladb.core.storage.tx.concurrency.conservative.PrimaryKey` were added, including a correction for a typo in the `PrimaryKey` import path.
    *   **3:02:53 PM**: Two new abstract methods, `getReadSet()` and `getWriteSet()`, were added, initially returning empty sets, to allow stored procedures to declare their read/write dependencies.
    *   **3:03:09 PM - 3:03:35 PM**: Comments were added/modified for `getReadSet()` and `getWriteSet()`, emphasizing their "new implement" status and the assumption that "only primary key is used for concurrency control."

*   **`c:\NTHU\Junior_II\DBSM2\hw5\hw5\core-patch\src\main\java\org\vanilladb\core\sql\storedprocedure\StoredProcedure.java`**
    *   **3:04:17 PM**: The `StoredProcedure` abstract class was updated to import `ConservativeConcurrencyMgr`, indicated by a "new implement" comment.
    *   **3:05:15 PM - 3:05:34 PM**: Comments within the `execute()` method were added and updated, indicating intent to "get read/write set from param helper and prepare concurrency control."
    *   **3:05:49 PM**: The `execute()` method was modified to integrate the new concurrency control logic. It now checks if the transaction's concurrency manager is a `ConservativeConcurrencyMgr` instance. If so, it casts it and calls its `prepareSp()` method, passing the `readSet` and `writeSet` obtained from `paramHelper`. The original `executeSql()` call was commented out.
    *   **3:06:07 PM - 3:07:29 PM**: No further functional code changes were made to this file.

*   **`c:\NTHU\Junior_II\DBSM2\hw5\hw5\bench\src\main\java\org\vanilladb\bench\server\param\micro\MicroTxnProcParamHelper.java`**
    *   **3:13:22 PM**: Initial commit of `MicroTxnProcParamHelper`, extending `StoredProcedureParamHelper` for micro-transaction benchmarks. It includes parameters for read/write operations on item IDs and prices.
    *   **3:13:43 PM**: Corrected a typo in the import path for `PrimaryKey`.
    *   **3:14:25 PM - 3:14:33 PM**: Introduced private `readSet` and `writeSet` fields (HashSets of `PrimaryKey`) with a comment indicating these are for concurrency control using primary keys.
    *   **3:16:04 PM - 3:16:36 PM**: Added a private helper method `makeItemKey(int itemId)` to construct `PrimaryKey` objects for "item" table. Overrode `getReadSet()` and `getWriteSet()` to return the new `readSet` and `writeSet` fields respectively.
    *   **3:17:32 PM - 3:17:47 PM**: The `prepareParameters()` method was updated to populate the `readSet` and `writeSet` by creating `PrimaryKey` instances for each read and write item ID provided in the transaction parameters. It also dynamically sets the `isReadOnly` flag based on the `writeCount`.

*   **Configuration Files (`.properties`)**:
    *   **`c:\NTHU\Junior_II\DBSM2\hw5\hw5\bench\src\main\resources\org\vanilladb\core\vanilladb.properties` (3:21:19 PM)** and **`c:\NTHU\Junior_II\DBSM2\hw5\hw5\core-patch\src\test\resources\org\vanilladb\core\vanilladb.properties` (3:21:44 PM)**:
        *   Both properties files explicitly configure `org.vanilladb.core.storage.tx.TransactionMgr.SERIALIZABLE_CONCUR_MGR` to use `org.vanilladb.core.storage.tx.concurrency.ConservativeConcurrencyMgr`. This confirms the integration of the new concurrency manager for serializable transactions.
        *   Differences exist in buffer pool size (`BUFFER_POOL_SIZE`: 102400 in bench, 1024 in test) and lock/buffer manager max waiting times (`MAX_TIME`: 10000 in bench, 1000 in test), suggesting distinct configurations for benchmark and testing environments.
        *   The `test` properties file also defines an `SP_FACTORY` which is absent in the `bench` version.

**Patterns and Recurring Elements:**

1.  **"// new implement" comments**: This phrase or similar (e.g., "// new implement, change", "// new implement, get read/write set") is consistently used across multiple files (`PrimaryKey.java`, `ConservativeConcurrencyMgr.java`, `StoredProcedureParamHelper.java`, `StoredProcedure.java`, `MicroTxnProcParamHelper.java`) to mark sections of code related to this new concurrency control implementation.
2.  **Focus on Conservative Concurrency Control**: The entire log revolves around implementing and integrating a conservative concurrency control manager. This includes defining `PrimaryKey` objects as the granularity for locking, developing a manager (`ConservativeConcurrencyMgr`) to handle lock acquisition, and modifying stored procedure interfaces (`StoredProcedureParamHelper`, `StoredProcedure`) to utilize this manager by exposing read/write sets.
3.  **Refactoring and Incremental Development**: Classes such as `PrimaryKey` and `ConservativeConcurrencyMgr` show an initial skeletal or basic implementation quickly evolving through subsequent commits to a more robust and complete feature set.
4.  **Integration through Interfaces**: The changes demonstrate how the `StoredProcedureParamHelper` and `StoredProcedure` classes were modified to provide and utilize transaction-specific read/write sets, acting as crucial integration points for the conservative concurrency manager.
5.  **Timestamp Consistency**: All changes were recorded on the same date, May 18, 2026, within a relatively short period (approximately 45 minutes), indicating a concentrated effort on this feature.
6.  **Minor Code Quirks**: The commented-out `package` declaration in `PrimaryKey.java` and the commented-out `executeSql()` call in `StoredProcedure.java` might represent temporary development states or incomplete steps that were logged.
7.  **Typo Corrections**: Small fixes like correcting import statements (e.g., `conservative.;PrimaryKey;` to `conservative.PrimaryKey;`) appear.