# Activity Summary for 5/31/2026

## 5:59:23 AM
The changes log details recent development within an Online Judge (OJ) project, primarily focusing on frontend features and continuous integration. All modifications occurred between the evening of May 30, 2026, and early morning of May 31, 2026.

### File-specific Updates:

*   **`.github\workflows\ci.yml` (Timestamp: 5/30/2026, 9:26:23 PM)**
    A GitHub Actions CI workflow named "CI" has been established. It triggers on `push` and `pull_request` to the `main` branch, as well as via `workflow_dispatch`. The workflow includes two main jobs:
    *   **Backend (pytest):** Runs on `ubuntu-latest` in the `backend` directory. It checks out code, sets up `uv` (a Python package installer/resolver) with caching, installs dependencies, and executes `pytest` for testing.
    *   **Frontend (tsc + vitest + build):** Runs on `ubuntu-latest` in the `frontend` directory. It checks out code, sets up Node.js v20 with npm caching, installs dependencies using `npm ci`, performs TypeScript type checking (`npx tsc --noEmit`), runs tests with `npm test`, and builds the frontend application (`npm run build`).

*   **`frontend\src\pages\ProblemEditor.tsx` (Timestamp: 5/30/2026, 10:30:21 PM)**
    This is a comprehensive React TypeScript component for an online problem editor.
    *   It integrates the `@monaco-editor/react` component for code editing.
    *   Supports multiple languages (Python 3, C++17) with predefined starter code.
    *   Features include fetching problem details (`apiGetProblem`), submitting solutions (`apiCreateSubmission`), and polling for submission results (`useSubmissionPoller`).
    *   The UI displays problem statements (title, description, input/output formats, sample cases), allows language selection, and shows submission feedback (status, verdict, score, runtime, memory, errors) using `VerdictBadge` and `ResultMetric` components.
    *   Crucially, it implements client-side persistence: saving code drafts to `localStorage` based on `examId`, `problemId`, and `language`, and reusing code from previous submissions via `sessionStorage` or URL parameters.

*   **`frontend\tests\CandidateDashboard.test.tsx` (Timestamp: 5/31/2026, 12:48:13 AM)**
    A Vitest test suite for the `CandidateDashboard` React component, designed for candidate users.
    *   It uses `@testing-library/react` and `userEvent` for UI interaction simulation.
    *   Mocks `useAuth` to simulate a logged-in candidate user and mocks various API calls (`apiListExams`, `apiListExamProblems`, `apiListSubmissions`) to control test data.
    *   Tests verify:
        *   Correct rendering of the exam table for a candidate, including active, upcoming, and ended exams, problem counts, and submission statuses (e.g., 'AC').
        *   The absence of staff-specific management links ("Manage", "Results") for candidates.
        *   Functionality for filtering exams by status (e.g., 'Upcoming') and searching by text.

### Patterns and Recurring Elements:

*   **Continuous Integration Focus:** The `ci.yml` demonstrates a robust CI pipeline covering both backend (Python) and frontend (TypeScript/Node.js) development, indicating a commitment to automated testing and quality assurance.
*   **Frontend Sophistication:** There is a strong emphasis on a rich frontend experience, evident in the `ProblemEditor`'s features (Monaco editor, language selection, client-side persistence, real-time feedback) and the detailed testing of the `CandidateDashboard`.
*   **API-driven Architecture:** Frontend components heavily rely on interactions with a backend API (e.g., `apiGetProblem`, `apiCreateSubmission`, `apiListExams`), with these interactions being consistently mocked for unit testing.
*   **Authentication and Authorization:** The `useAuth` hook and its mocking in tests highlight a centralized authentication mechanism, with specific user roles (like "candidate") impacting UI rendering and functionality.
*   **Code Quality:** The use of TypeScript for type checking (`npx tsc --noEmit`) and dedicated test files (`.test.tsx`) with comprehensive mocks and assertions points to a focus on maintaining high code quality and reliability.
*   **Timeliness:** All changes occurred within a very narrow window, suggesting a focused work session or feature rollout.

## 7:59:22 AM
The code changes primarily focus on the frontend development and continuous integration setup for an online judge (OJ) system, occurring around **May 30-31, 2026**.

**File-Specific Updates:**

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\.github\workflows\ci.yml` (Timestamp: 5/30/2026, 9:26:23 PM):**
    A GitHub Actions CI workflow was established. It defines two main jobs:
    *   `backend`: Runs `pytest` tests for the Python backend using `uv` for dependency management and caching.
    *   `frontend`: Performs `tsc` type checking, `vitest` unit tests, and `npm build` for the frontend, using Node.js v20 and caching `npm` dependencies.
    This workflow triggers on `push` and `pull_request` to the `main` branch, and can also be dispatched manually.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ProblemEditor.tsx` (Timestamp: 5/30/2026, 10:30:21 PM):**
    This file defines the `ProblemEditor` React component, which is the user interface for solving programming problems. Key updates include:
    *   **Multi-language support:** Configured for Python 3 and C++17, including specific Monaco editor settings and starter code.
    *   **Code persistence:** Implements saving of user code drafts to `localStorage` based on exam, problem, and language, and loading previously saved drafts.
    *   **Submission reuse:** Allows loading code from a specific past submission (identified by `submissionId` in search params) into the editor.
    *   **Problem display:** Renders comprehensive problem details including title, time/memory limits, description, input/output formats, and sample cases.
    *   **Real-time feedback:** Integrates with `useSubmissionPoller` to display live submission status, verdict, score, passed test cases, runtime, memory usage, and error messages.
    *   The component handles fetching problem data, submitting code, and managing UI state for language selection and submission process.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\CandidateDashboard.test.tsx` (Timestamp: 5/31/2026, 12:48:13 AM):**
    New Vitest unit tests were added for the `CandidateDashboard` component. These tests ensure:
    *   **Correct rendering for candidates:** Verifies that the dashboard correctly displays active, upcoming, and ended exams, along with problem counts and the last submission status (e.g., 'AC' for Accepted).
    *   **Role-based access control:** Explicitly asserts that staff-specific management and results links are *not* visible to candidate users.
    *   **Filtering functionality:** Tests the ability to filter exams by status (e.g., 'Upcoming') and by search text, ensuring the UI responds correctly to user input.
    The tests use mocked API calls and `useAuth` hook to simulate a candidate user and various exam/submission scenarios.

**Patterns and Recurring Elements:**

*   **Frontend-centric development:** The majority of changes are focused on the frontend, specifically enhancing the candidate experience with the problem editor and dashboard.
*   **Emphasis on testing:** A strong commitment to testing is evident with the detailed unit tests for the `CandidateDashboard`, ensuring correctness and robustness of UI features.
*   **Modern development tooling:** The adoption of `uv` for Python dependency management and `vitest` for frontend testing indicates a preference for modern and efficient build/test tools.
*   **User experience and data persistence:** Features like local draft saving and submission reuse in `ProblemEditor` highlight a focus on improving the user workflow and preventing data loss.
*   **Clear separation of concerns:** The CI workflow defines distinct jobs for backend and frontend, reflecting a modular architecture.
*   **Security/Permissions consideration:** Tests explicitly checking for the absence of staff links for candidates indicate careful implementation and verification of role-based access in the UI.

## 8:59:21 AM
The changes log details recent development activity across continuous integration, a key frontend problem-solving page, and frontend testing.

On **May 30, 2026, at 9:26:23 PM**, a new **`ci.yml`** workflow was introduced in `.github/workflows`. This GitHub Actions configuration establishes a continuous integration pipeline that triggers on pushes and pull requests to the `main` branch, and also manually via `workflow_dispatch`. It defines two primary jobs:
*   **Backend (pytest):** Runs on `ubuntu-latest`, navigates to the `backend` directory, checks out code, sets up `uv` (a Python package manager) with caching, installs dependencies (`uv sync --frozen --extra dev`), and executes backend tests (`uv run pytest -q`).
*   **Frontend (tsc + vitest + build):** Also runs on `ubuntu-latest`, navigates to the `frontend` directory, checks out code, sets up Node.js v20 with `npm` caching, installs frontend dependencies (`npm ci`), performs type checking (`npx tsc --noEmit`), runs tests (`npm test`), and builds the frontend application (`npm run build`).

Later on **May 30, 2026, at 10:30:21 PM**, significant updates were made to **`frontend\src\pages\ProblemEditor.tsx`**. This file implements the core problem editor interface. Key updates include:
*   Integration of the `@monaco-editor/react` component for code editing.
*   Definition of `LANGUAGE_OPTIONS` for Python 3 and C++17, including display labels, Monaco editor language IDs, and starter code snippets.
*   Functionality to manage user code drafts in `localStorage` (`draftKey`, `loadDraft`), ensuring user progress is saved.
*   Ability to load and reuse code from previous submissions stored in `sessionStorage` (`loadReusableSubmission`).
*   Two new functional components:
    *   `ProblemStatement`: Responsible for displaying problem details such as title, time/memory limits, description, input/output formats, and sample cases.
    *   `SubmissionPanel`: Shows the real-time status and results of a code submission, including verdict, score, passed tests, runtime, memory usage, and any error messages, with a "Waiting for judge..." animation for active submissions.
*   The main `ProblemEditor` component handles fetching problem data, managing selected language, code content, submission state, and polling for submission results. It dynamically adjusts available languages based on the problem configuration and utilizes `useAuth` for authentication.

Finally, on **May 31, 2026, at 12:48:13 AM**, a new test file, **`frontend\tests\CandidateDashboard.test.tsx`**, was added. This file introduces comprehensive unit tests for the `CandidateDashboard` component using `vitest` and `@testing-library/react`.
*   It extensively mocks API calls (`apiListExams`, `apiListExamProblems`, `apiListSubmissions`) and the `useAuth` hook to simulate a candidate user's environment.
*   Helper functions generate mock exam data (active, upcoming, ended) and submission data.
*   The tests verify two main functionalities:
    *   Correct rendering of the exam table for candidates, including details like exam title, start/end times, problem counts, and last submission status. Crucially, it asserts that staff-specific management links ("Manage", "Results") are absent for candidate users.
    *   The ability to filter exams by status (e.g., 'Upcoming') and search text, ensuring the dashboard dynamically updates to show only matching exams or a "No exams match" message.

**Overall Patterns and Recurring Elements:**
The development log shows a focused effort on solidifying the application's core. The consistent use of `useAuth` across frontend components indicates a strong emphasis on user authentication and role-based access. Frontend changes leverage React hooks (`useState`, `useEffect`, `useMemo`) for robust state management and component lifecycle control. The timestamps suggest continuous development, with CI/CD setup, a major feature implementation, and corresponding tests being added in close succession. The `uv` package manager for Python and `npm` for Node.js are recurring tools across the CI and frontend development.

## 2:04:47 PM
The provided log details recent development activity across a GitHub Actions workflow, a core frontend component, and its associated tests. All changes occurred between 9:26 PM on May 30, 2026, and 12:48 AM on May 31, 2026, suggesting a focused burst of development.

**File-specific updates:**

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\.github\workflows\ci.yml` (Timestamp: 5/30/2026, 9:26:23 PM):** This file defines a Continuous Integration (CI) workflow for the project. It triggers on pushes and pull requests to the `main` branch, and manually via `workflow_dispatch`. The workflow includes two main jobs:
    *   **Backend (pytest):** Runs on `ubuntu-latest`, uses `uv` to install Python dependencies (including dev extras), and executes `pytest` for testing.
    *   **Frontend (tsc + vitest + build):** Also runs on `ubuntu-latest`, sets up Node.js v20, uses `npm` for dependency installation, performs TypeScript type checking (`npx tsc --noEmit`), runs `npm test`, and finally builds the frontend (`npm run build`).

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ProblemEditor.tsx` (Timestamp: 5/30/2026, 10:30:21 PM):** This is a significant frontend component for an online judge system, allowing candidates to view problems, write code, and submit solutions. Key features include:
    *   Integration of `Monaco Editor` for code editing.
    *   Support for Python 3 and C++17, defining starter code and Monaco language IDs for each.
    *   **Draft Persistence:** Implements `localStorage` to save and load user code drafts, unique per exam, problem, and language.
    *   **Submission Reuse:** Allows loading code from a previous submission using `sessionStorage`.
    *   **Problem Statement Display:** Renders problem details like title, time/memory limits, description, input/output formats, and sample cases.
    *   **Submission Feedback:** Displays a `SubmissionPanel` showing the current status (pending, judging, completed), verdict (e.g., "Accepted"), score, passed test cases, runtime, memory usage, and any error messages.
    *   Utilizes `react-router-dom` for navigation, custom API hooks (`apiGetProblem`, `apiCreateSubmission`), and a `useSubmissionPoller` hook to track submission status in real-time.
    *   Handles language selection, code changes, and the submission process.

*   **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\CandidateDashboard.test.tsx` (Timestamp: 5/31/2026, 12:48:13 AM):** This file contains unit tests for the `CandidateDashboard` component using `@testing-library/react` and `vitest`.
    *   It includes helper functions to generate mock exam and submission data and to mock the `useAuth` hook for a candidate user.
    *   Mocks API calls for listing exams, exam problems, and submissions (`apiListExams`, `apiListExamProblems`, `apiListSubmissions`) to provide predictable data for tests.
    *   **Test Cases:**
        *   Verifies that the dashboard correctly renders a dense exam table displaying active, upcoming, and ended exams, along with problem counts and last submission status (e.g., "AC"), while ensuring staff-specific management links are *not* present for candidate users.
        *   Tests filtering functionality, confirming that exams can be filtered by status (e.g., "Upcoming") and searched by text, correctly updating the displayed list or showing a "No exams match" message.

**Patterns and Recurring Elements:**

*   **Online Judge System Focus:** The changes clearly revolve around developing and testing an Online Judge (OJ) platform, featuring problems, exams, code submissions, and real-time feedback.
*   **Full-stack Development (CI):** The CI workflow covers both backend (Python) and frontend (TypeScript/React) components, indicating a full-stack application.
*   **Frontend Development Emphasis:** Two out of the three entries are related to the frontend, focusing on a critical user-facing component (`ProblemEditor.tsx`) and its testing (`CandidateDashboard.test.tsx`).
*   **Modern Web Technologies:** Consistent use of React, TypeScript, `Monaco Editor` for the frontend, and `vitest` for testing, alongside GitHub Actions for CI/CD.
*   **User Experience (UX) Considerations:** The `ProblemEditor` demonstrates attention to UX by implementing features like code draft persistence and the ability to reuse previous submission code.
*   **Robust Testing:** The `CandidateDashboard` tests show a commitment to ensuring the frontend behaves as expected for candidates, including filtering capabilities and proper UI rendering.

## 3:04:49 PM
The provided log details recent changes across the project's CI configuration and frontend components, along with new tests.

**c:\NTHU\Junior_II\Web Vibecodiing\OJ\.github\workflows\ci.yml** (Timestamp: 5/30/2026, 9:26:23 PM)
This file defines a new GitHub Actions Continuous Integration (CI) workflow. It triggers on pushes and pull requests to the `main` branch, and also manually via `workflow_dispatch`. The workflow consists of two main jobs:
*   **Backend (pytest):** Runs on `ubuntu-latest`, checks out the code, sets up `uv` (a Python package manager) with caching, installs Python dependencies, and executes `pytest` for backend tests.
*   **Frontend (tsc + vitest + build):** Runs on `ubuntu-latest`, checks out the code, sets up Node.js v20 with `npm` caching, installs frontend dependencies, performs TypeScript type checking (`tsc --noEmit`), runs `vitest` for tests, and finally builds the frontend application.
This indicates a focus on automating code quality and build processes for both parts of the application.

**c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ProblemEditor.tsx** (Timestamp: 5/30/2026, 10:30:21 PM)
This React TypeScript component implements the "Problem Editor" page. Key functionalities include:
*   **Code Editing:** Integrates `@monaco-editor/react` for a rich code editing experience.
*   **Language Support:** Defines `LANGUAGE_OPTIONS` for Python 3 and C++17, providing labels, Monaco language IDs, and starter code.
*   **Draft Management:** Implements `draftKey` and `loadDraft` functions to persist user code drafts in `localStorage` based on exam, problem, and selected language, ensuring code isn't lost.
*   **Code Reuse:** Supports loading code from a previous submission via a URL parameter, stored in `sessionStorage` using `loadReusableSubmission`.
*   **Problem Statement Display:** A `ProblemStatement` sub-component renders problem details, including title, time/memory limits, description, input/output formats, and sample cases.
*   **Submission Feedback:** A `SubmissionPanel` sub-component displays real-time submission status, verdict, score, test case counts, runtime, memory usage, and any error messages, including a "Waiting for judge..." animation.
*   **API Interactions:** Uses `apiGetProblem` to fetch problem details and `apiCreateSubmission` to submit user code. It leverages `useAuth` for authentication and `useSubmissionPoller` to continuously check for submission results.
*   **Dynamic Language Selection:** Adjusts available languages based on `problem.allowed_langs` and automatically selects a valid language if the current one becomes unavailable.
This file represents a significant update to the core interactive problem-solving experience for candidates.

**c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\CandidateDashboard.test.tsx** (Timestamp: 5/31/2026, 12:48:13 AM)
This file introduces comprehensive `vitest` unit tests for the `CandidateDashboard` component. The tests verify key aspects of the candidate's dashboard:
*   **Mocking:** Heavily uses `vi.spyOn` to mock API calls (`apiListExams`, `apiListExamProblems`, `apiListSubmissions`) and the `useAuth` hook, isolating the component logic.
*   **Exam Display:** Verifies that the dashboard correctly renders a dense exam table, showing active, upcoming, and ended exams with relevant details (start/end times, problem counts, last submission status).
*   **Role-Based UI:** Explicitly asserts that staff-specific management links ("Manage", "Results") are *not* displayed for candidate users, ensuring correct access control in the UI.
*   **Filtering Functionality:** Tests user interaction to filter exams by status (e.g., "Upcoming") and by search text, confirming that the display updates accurately.
These tests ensure the `CandidateDashboard` functions as expected for candidates, with correct data presentation and UI interactions.

**Key Information & Patterns:**

*   **Project Structure:** The log shows a clear distinction between `backend` and `frontend` components, managed by separate CI jobs.
*   **Frontend Focus:** The changes indicate significant development on the frontend, particularly for the problem-solving and dashboard user interfaces.
*   **Technology Stack:** The project utilizes a modern web stack including React, TypeScript, Node.js (with `npm`), `monaco-editor` for advanced text editing, and `vitest` for testing. Python is used for the backend with `pytest` and `uv`.
*   **Client-Side Persistence:** `localStorage` and `sessionStorage` are employed in `ProblemEditor.tsx` for enhancing user experience by saving drafts and enabling temporary code reuse.
*   **API-Driven:** Both the `ProblemEditor` and the `CandidateDashboard` (as evidenced by its tests) rely heavily on API calls for data retrieval and submission. Authentication via `useAuth` and access tokens is a recurring pattern.
*   **Automated Quality:** The new CI workflow demonstrates a commitment to automated testing (both backend and frontend) and building processes, aiming for continuous integration.
*   **User Experience:** Features like code drafting, submission polling, and clear display of problem/submission data point to a focus on a smooth user experience.
*   **Timestamps:** All major changes occurred within a short timeframe between late evening on May 30th, 2026, and early morning on May 31st, 2026, suggesting a concentrated development effort.