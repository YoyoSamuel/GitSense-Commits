# Activity Summary for 5/15/2026

## 12:18:04 AM
The `README.md` file for the "RoyalRoad Reader-Comment Generator" project was updated on **5/14/2026, 2:56:36 PM**.

This `README.md` provides a comprehensive overview of a pipeline designed to scrape RoyalRoad chapters and reader comments, process them into training data, fine-tune a Qwen-based LLM using LoRA, and then use this model to generate realistic reader comments for any given chapter text.

The document details a six-step pipeline:

1.  **`chapter_scraper.py`**: Scrapes top-rated fictions, saving chapter content to `chapters_dataset.jsonl`. It supports resuming and has tunable constants for politeness and limits.
2.  **`comment_scraper.py`**: Crawls best-rated pages to scrape reader comments for each chapter, storing them in `comments_dataset.jsonl`. It automatically resumes from existing data.
3.  **`merge_comments.py`**: Distills noisy real reader comments by grouping them and using an Ollama-cloud model (`gemma4:31b-cloud`) to merge batches into single, casual-toned comments, outputting `comments_merged.jsonl`. This step requires an `OLLAMA_API_KEY`.
4.  **`prepare_sft_data.py`**: Joins chapters with the merged comments to build chat-format SFT (Supervised Fine-Tuning) examples, saving them to `sft_data/train.jsonl`. Chapters are intelligently truncated to focus on the beginning and ending.
5.  **`train_comment_sft.py`**: Fine-tunes a Qwen base model (defaulting to `Qwen3.5-9B`) with QLoRA (4-bit quantization, bf16 LoRA) using TRL's `SFTTrainer`. It logs to Weights & Biases and outputs intermediate checkpoints and a final LoRA adapter. A specific `HF_HOME` environment variable is noted as a hardcoded line that might need adjustment.
6.  **`generate_comments.py`**: The final script for generating comments using either the base model or the fine-tuned LoRA adapter. It takes various inputs like chapter files or inline text, metadata, and generation parameters (e.g., number of comments, temperature).

The `README.md` also includes a "Quick start" guide for inference only, outlining steps for cloning, installing dependencies, logging into Hugging Face (if models are gated), setting HF cache, downloading the LoRA adapter, and running `generate_comments.py`. Instructions for sharing the trained checkpoint via Hugging Face Hub are also provided, requiring `huggingface_hub` and `huggingface-cli login`.

**Key patterns and recurring elements across the pipeline include:**

*   **Data Flow:** A clear progression from raw scraped data (`.jsonl` files) to processed datasets, then to training, and finally inference.
*   **LLM Integration:** Multiple LLMs are used: an Ollama-cloud model for comment merging and a Qwen-based model for fine-tuning and generation.
*   **Hugging Face Ecosystem:** Heavily utilized for base models, LoRA adapters, and checkpoint sharing, often requiring `huggingface-cli login` for authenticated access.
*   **Command-line Utility:** All scripts are designed to be run from the command line with various configurable flags.
*   **Resumption and Efficiency:** Scrapers and the merge script are built with resumption capabilities to avoid reprocessing existing data.
*   **Environment Variables:** Critical settings like `HF_HOME`, `OLLAMA_API_KEY`, and `CUDA_VISIBLE_DEVICES` are managed via environment variables.

## 2:18:06 AM
The `c:\NTHU\Junior_I\Data Lab\RoyalRoad\README.md` file, last updated on 5/14/2026, 2:56:36 PM, introduces the "RoyalRoad Reader-Comment Generator" project. This project outlines a comprehensive pipeline to scrape chapters and reader comments from RoyalRoad, process them into training data, fine-tune a Qwen-based LLM using LoRA, and then use the model to generate authentic-sounding reader comments.

The README provides a detailed breakdown of the project's components:

*   **Pipeline Overview:** A diagram illustrates the flow from `chapter_scraper.py` and `comment_scraper.py` to various `.jsonl` datasets, `prepare_sft_data.py`, `merge_comments.py`, `train_comment_sft.py`, and finally `generate_comments.py`.
*   **Quick Start (Inference):** Instructions are given for running the `generate_comments.py` script with a pre-trained LoRA adapter. This includes cloning the repository, installing dependencies, optionally logging into Hugging Face, setting the `HF_HOME` cache, downloading the `SamuelYo/royalroad-comment-sft` adapter, and running the generation script with parameters like `--mode sft`, `--chapter-file`, `--fiction`, and `--num-comments`. GPU pinning with `CUDA_VISIBLE_DEVICES` is also noted.
*   **Full Pipeline Scripts:**
    *   **`chapter_scraper.py`:** Scrapes top-rated fictions, saving chapter content to `chapters_dataset.jsonl`. It supports configurable limits (`TOP_N_FICTIONS`, `CHAPTER_LIMIT`) and resumes automatically.
    *   **`comment_scraper.py`:** Scrapes reader comments for chapters, outputting to `comments_dataset.jsonl`. It targets a specific number of new comments (`TARGET_NEW_COMMENTS`) and also supports automatic resume.
    *   **`merge_comments.py`:** Processes raw comments, using an Ollama-cloud model (`gemma4:31b-cloud`) to merge batches of noisy comments into single, clean, single-voice comments. It requires an `OLLAMA_API_KEY` and outputs to `comments_merged.jsonl`, also supporting resume.
    *   **`prepare_sft_data.py`:** Creates chat-format SFT (Supervised Fine-Tuning) training examples by joining chapters with merged comments into `sft_data/train.jsonl`. It includes options for chapter truncation and minimum comment length.
    *   **`train_comment_sft.py`:** Fine-tunes the Qwen base model (Qwen/Qwen3.5-9B) using QLoRA and TRL's `SFTTrainer`. It logs to Weights & Biases (W&B) by default and supports resuming from checkpoints. A hardcoded `HF_HOME` path is highlighted, requiring user attention.
    *   **`generate_comments.py`:** The final script for generating comments, with detailed flags for controlling generation parameters, model loading (`--mode sft`/`--mode base`), and input chapter content.
*   **Sharing Checkpoints:** Guidance is provided on how to upload the locally trained LoRA adapter (located in `checkpoints/comment_sft/final/`) to the Hugging Face Hub using `huggingface-cli`.

**Patterns and Recurring Elements:**
*   **Data Flow:** The pipeline consistently processes data through `.jsonl` files at each major step (`chapters_dataset.jsonl`, `comments_dataset.jsonl`, `comments_merged.jsonl`, `sft_data/train.jsonl`).
*   **Resume Functionality:** Multiple scraping and processing scripts (`chapter_scraper.py`, `comment_scraper.py`, `merge_comments.py`) are designed with resume capabilities to handle interruptions and incremental data collection efficiently.
*   **Hugging Face Integration:** Hugging Face is central for model base downloads, LoRA adapter sharing, and potentially for gated models, requiring `huggingface-cli login` and `HF_HOME` configuration.
*   **LLM Choice:** The Qwen/Qwen3.5-9B model is consistently chosen as the base LLM for fine-tuning.
*   **LoRA Adapters:** The LoRA fine-tuning method is a core technique, with the adapter stored in `checkpoints/comment_sft/final/` for both training output and inference input.
*   **Environment Variables:** Several environment variables (`HF_HOME`, `OLLAMA_API_KEY`, `CUDA_VISIBLE_DEVICES`) are used for configuration, highlighting the need for proper environment setup.