# Activity Summary for 6/5/2026

## 3:33:25 AM
The provided log details changes across two key files, primarily focusing on Docker Compose configurations for an Online Judge (OJ) system and frontend testing for exam management.

### File-Specific Updates:

1.  **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\docker-compose.yml` (Timestamp: 6/4/2026, 4:52:10 PM)**
    This Docker Compose file sets up a comprehensive Online Judge (OJ) application environment, encompassing multiple services:
    *   **Core Services:** `postgres` (version 16-alpine), `redis` (version 7-alpine), and `minio` (S3-compatible object storage) are configured with health checks, persistent volumes, and default credentials.
    *   **Application Services:**
        *   `api`: The backend API, which performs database migrations (`alembic upgrade head`) and runs the `uvicorn` ASGI server. It depends on `postgres`, `redis`, and `minio`.
        *   `judge-worker`, `judge-monitor`, `run-worker`: These are distinct worker services. `judge-worker` and `run-worker` are configured with `privileged: true`, `seccomp:unconfined`, and mount `/var/run/docker.sock`, indicating their role in sandboxed execution of user code, typical for an OJ system. All workers use a `Dockerfile.worker` and rely on `postgres` and `redis`. `judge-monitor` specifically runs a `worker.py monitor` command, and `run-worker` uses `rq worker`.
    *   **Frontend & Monitoring:**
        *   `nginx`: Serves the frontend, configured to listen on port 80 and depends on the `api` service.
        *   `prometheus` (v2.53.0) and `grafana` (v11.1.0): Integrated for observability, exposing ports 9090 and 3000 respectively. Grafana is provisioned with dashboards and data sources and comes with default admin credentials. Both utilize persistent volumes and depend on relevant application services for metric collection.
    *   **Configuration:** Services like `api` and the workers are configured to use an `.env` file for environment variables and connect to the database, Redis, and MinIO endpoints.

2.  **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\ExamManagePage.test.tsx` (Timestamp: 6/4/2026, 4:52:23 PM)**
    This file contains Vitest tests for the `ExamManagePage` React component, focusing on its functionality for creating, editing, and managing exams.
    *   **Setup:** Extensive mocking is used for `react-router-dom`'s `useNavigate`, the custom `useAuth` hook (simulating an admin user), and various API calls (`apiListProblems`, `apiListAdminUsers`, `apiGetExam`, `apiCreateExam`, `apiCreateAssignment`, `apiDeleteExam`).
    *   **Initial Rendering Tests:** Verifies correct rendering for new exam forms and existing exam editing views, including the presence of "Create exam," "Save changes," and "Delete exam" buttons.
    *   **Form Interaction Tests:**
        *   Confirms client-side validation for required fields.
        *   Simulates the creation of a new exam, including filling form details, selecting problems and candidates, and asserting that the `apiCreateExam` and `apiCreateAssignment` functions are called correctly, followed by navigation.
        *   Includes a test for deleting an existing exam, asserting `confirm` dialog interaction and `apiDeleteExam` invocation.
    *   **Filtering and Bulk Actions Tests:** Verifies the functionality of filtering problems by text and difficulty, filtering candidates by text, and using "Select all" and "Clear" buttons for both problem and candidate lists.

### Patterns and Recurring Elements:

*   **Comprehensive Dockerization:** The project utilizes Docker Compose to orchestrate a multi-service application, including a database, cache, object storage, API, multiple worker roles, frontend server, and dedicated monitoring tools. This indicates a focus on scalable and maintainable deployment.
*   **Microservices Architecture:** The application is broken down into distinct services (API, judge workers, etc.), communicating over the network, which is a common pattern for complex web applications like an OJ system.
*   **Robust Testing in Frontend:** The frontend component `ExamManagePage` is thoroughly tested using `@testing-library` and `vitest`, with extensive mocking of APIs, authentication, and routing, demonstrating a commitment to high-quality frontend development.
*   **Environment-based Configuration:** The use of `.env` files for environment variables (referenced in `docker-compose.yml`) is a standard practice for managing configurations across different deployment environments.
*   **Online Judge Specifics:** The presence of `judge-worker` and `run-worker` services with privileged Docker access and socket mounting strongly points to the application being an Online Judge system, where user-submitted code needs to be executed securely in isolated environments.
*   **Observability Stack:** The inclusion of Prometheus and Grafana indicates a proactive approach to monitoring the application's health and performance.

## 7:03:41 AM
The `docker-compose.yml` file, timestamped `6/4/2026, 4:52:10 PM`, configures a multi-service environment for an Online Judge (OJ) system, including `postgres`, `redis`, `minio`, `api` (backend), several `judge` related worker services, `nginx` (frontend), `prometheus`, and `grafana`. This file contains sensitive environment variables and credentials directly embedded or referenced via `.env` files, and as such, its detailed content will not be summarized.

The `c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\ExamManagePage.test.tsx` file, updated on `6/4/2026, 4:52:23 PM`, provides comprehensive unit tests for the `ExamManagePage` React component. Key updates and functionalities covered in these tests include:

*   **Initial Rendering Scenarios:** Tests verify the correct rendering of the form for creating a new exam and loading an existing exam for editing. It checks for the presence of specific UI elements like "New Exam," "Edit: [Exam Title]," "Create exam" button, and "Delete exam" and "Save changes" buttons for existing exams.
*   **Form Interactions and Saving:**
    *   **Validation:** Ensures that required fields (title, start time, end time) trigger validation messages if left empty upon saving.
    *   **New Exam Creation:** Simulates user input to create a new exam, including typing title and description, setting start/end times via `fireEvent` for `datetime-local` inputs, toggling "Show score," and selecting problems and candidates. It asserts that `apiCreateExam` and `apiCreateAssignment` are called with the correct data and that the user is navigated to the new exam's manage page.
    *   **Exam Deletion:** Includes a test case (part of an "updates existing exam" block) that simulates clicking the "Delete exam" button, confirming the deletion prompt, and verifying calls to `apiDeleteExam` and navigation back to the exams list.
*   **Filtering and Bulk Actions:**
    *   **Problem Filtering:** Tests the ability to filter problems by text search (e.g., "Hard" finds "Hard Problem") and by difficulty (e.g., "easy" finds "Problem 1"), confirming that non-matching problems are hidden. It also checks for "No matching problems" message.
    *   **Candidate Filtering:** Verifies that candidates can be filtered by text search (e.g., "alice@example" finds "Alice"), ensuring non-matching candidates are hidden and displaying "No matching candidates" when appropriate.
    *   **Select/Clear All:** Tests the "Select all" and "Clear" buttons for both problems and candidates, verifying that the displayed count of selected items updates correctly.

The tests extensively use mocks for API calls (`examsApi`, `adminApi`, `problemsApi`) and authentication (`useAuthModule`) to ensure isolated and reliable testing of the `ExamManagePage` component's behavior. The mock data often uses future-dated timestamps like `2026-06-01T...` for example exam and problem creation dates.

**Patterns and Recurring Elements:**
*   A clear focus on robust frontend testing is evident, utilizing Vitest and `@testing-library/react` for comprehensive UI and interaction tests.
*   The project structure suggests a distinct frontend (with tests like this) and backend (implied by the `docker-compose.yml` services like `api`, `judge-worker`).
*   The use of `mockResolvedValue` for API calls is a recurring pattern for isolating frontend components from actual backend interaction during testing.
*   The presence of `useAuthModule` and `apiListAdminUsers` indicates a system with user authentication and role-based access control (admin/candidate roles).

## 12:03:26 PM
The log details changes across two files related to an Online Judge (OJ) system, both timestamped on 6/4/2026 within seconds of each other, indicating a focused development effort.

**File: `c:\NTHU\Junior_II\Web Vibecodiing\OJ\docker-compose.yml` (Timestamp: 6/4/2026, 4:52:10 PM)**
This file configures a comprehensive Docker Compose setup for the OJ system's development environment. It defines multiple services:
*   **Core Infrastructure:** `postgres` (version 16-alpine), `redis` (version 7-alpine), and `minio` (latest object storage) are configured with port mappings, data volumes, and health checks. Development-specific credentials are set directly within the compose file for PostgreSQL and MinIO access.
*   **Backend Services:**
    *   `api`: The main backend API, which builds from a local `Dockerfile` and is responsible for running database migrations (`alembic upgrade`) before starting the Uvicorn application. It has dependencies on `postgres`, `redis`, and `minio` being healthy.
    *   `judge-worker`, `judge-monitor`, `run-worker`: These are specialized backend worker services, all building from a `Dockerfile.worker`. Notably, `judge-worker` and `run-worker` are configured with `privileged: true`, `security_opt: - seccomp:unconfined`, and mount `/var/run/docker.sock`, indicating they are designed to interact directly with the Docker daemon, likely for executing user code submissions in isolated environments. All worker services depend on `postgres` and `redis`.
*   **Frontend & Monitoring:**
    *   `nginx`: Serves the frontend application, building from a local `Dockerfile`, exposing port 80, and mounting a custom `nginx.conf`. It depends on the `api` service.
    *   `prometheus` (v2.53.0) and `grafana` (v11.1.0): A monitoring and visualization stack is included, with respective port mappings, volume mounts for configurations and data. Grafana includes default administrator credentials set directly in the compose file.
*   **Volumes:** Dedicated Docker volumes (`postgres_data`, `minio_data`, `prometheus_data`, `grafana_data`) are defined for persistent data storage.

**File: `c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\ExamManagePage.test.tsx` (Timestamp: 6/4/2026, 4:52:23 PM)**
This file provides extensive Vitest unit and integration tests for the `ExamManagePage` component within the frontend.
*   **Testing Setup:** The tests leverage React testing utilities (`render`, `screen`, `userEvent`), `MemoryRouter` for managing routing context, and `vi.mock` for comprehensive mocking of external dependencies.
*   **Mocking:**
    *   `react-router-dom`'s `useNavigate` hook is mocked to control navigation programmatically.
    *   The `useAuth` hook is spied on to simulate an authenticated admin user, providing a test `accessToken`.
    *   Various API calls from `examsApi`, `adminApi`, and `problemsApi` are mocked to return predefined test data for exams, problems, and candidate users, ensuring isolated testing of the component's logic.
*   **Test Scenarios:** The tests cover key functionalities of the `ExamManagePage`:
    *   **Initial Rendering:** Verifies that the page correctly renders forms for creating new exams and displays existing exam details for editing, including the presence of relevant action buttons (e.g., "Create exam", "Save changes", "Delete exam").
    *   **Form Interactions and Saving:** Includes tests for input validation (e.g., required fields), simulating user input for exam details (title, description, start/end times, show score), selecting problems and candidates, and asserting that the correct API calls (`apiCreateExam`, `apiCreateAssignment`) are made upon submission, followed by appropriate navigation. It also includes a test verifying the deletion of an existing exam.
    *   **Filtering and Bulk Actions:** Tests the functionality of filtering problems by text and difficulty, filtering candidates by text, and the "Select all" and "Clear" buttons for managing problem and candidate assignments, verifying changes in selected item counts.

**Patterns and Key Information:**
The tight timestamps suggest these files were updated as part of a closely related feature or iteration. The project involves a full-stack Online Judge system, with `docker-compose.yml` providing the development environment for a microservices architecture including specialized code execution workers. The `ExamManagePage.test.tsx` demonstrates a robust approach to frontend testing, heavily relying on mocking to isolate component behavior and covering a wide range of user interactions and data management scenarios. A recurring pattern observed in the `docker-compose.yml` is the direct definition of development-level credentials for services like PostgreSQL, MinIO, and Grafana. The extensive use of mocking in the frontend tests is also a prominent pattern.

## 2:33:46 PM
The provided log details significant updates to a Docker Compose configuration and a React component's test suite, all occurring on June 4, 2026.

The `c:\NTHU\Junior_II\Web Vibecodiing\OJ\docker-compose.yml` file, timestamped at 4:52:10 PM, defines a comprehensive multi-service application stack for an Online Judge (OJ) system.
Key services include:
*   **Core Infrastructure:** `postgres` (database, version 16-alpine, exposed on 5432, with health check), `redis` (caching/broker, version 7-alpine, with health check), and `minio` (object storage, with console on 9001, health check). These all use dedicated persistent volumes (`postgres_data`, `minio_data`).
*   **Backend Application:** `api` (main API service, uses `backend` context and `Dockerfile`, runs database migrations and `uvicorn`), `judge-worker`, `judge-monitor`, and `run-worker` (all built from `backend` context using `Dockerfile.worker`, handle judging and background tasks, are `privileged`, and mount `/var/run/docker.sock`). All backend services source environment variables from a `.env` file and depend on `postgres`, `redis`, and `minio` being healthy.
*   **Frontend/Proxy:** `nginx` (web server/reverse proxy, built from `frontend` context, exposed on port 80, mounts a custom `nginx.conf`, depends on `api`).
*   **Observability:** `prometheus` (monitoring, exposed on 9090, mounts `prometheus.yml`, persistent volume `prometheus_data`, depends on `api`) and `grafana` (data visualization, version 11.1.0, exposed on 3000, with default admin credentials, mounts provisioning and dashboards, persistent volume `grafana_data`, depends on `prometheus`).
A recurring pattern is the use of `condition: service_healthy` for service dependencies, ensuring services start in the correct order after their dependencies are ready.

The `c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\ExamManagePage.test.tsx` file, timestamped at 4:52:23 PM, details an extensive test suite for the `ExamManagePage` React component.
The tests cover:
*   **Initial Rendering:** Verifying the display of new exam forms and the correct loading/rendering of existing exams, including the presence of "Create exam," "Save changes," and "Delete exam" buttons.
*   **Form Interactions and Saving:** Testing form validation for required fields, simulating the creation of a new exam (including inputting details, selecting problems and candidates), and verifying API calls (`apiCreateExam`, `apiCreateAssignment`) and navigation behavior. It also includes a test for deleting an existing exam, checking the confirmation prompt and the `apiDeleteExam` call.
*   **Filtering and Bulk Actions:** Testing the functionality of filtering problems by text and difficulty, filtering candidates by text, and using "Select all" and "Clear" buttons for both problem and candidate lists, asserting that the displayed counts update correctly.
The tests heavily rely on `vi` for mocking `react-router-dom`'s `useNavigate` and `useAuth` hooks, as well as all relevant API calls (`examsApi`, `adminApi`, `problemsApi`) to control test data and verify interactions. The `beforeEach` hook ensures a clean state with default mock data for problems and candidates before each test. `userEvent` is consistently used to simulate user interactions, while `waitFor` and `expect` are crucial for asserting asynchronous UI updates and API call specifics.

## 3:33:46 PM
The first log entry, dated **6/4/2026, 4:52:10 PM**, details changes to `c:\NTHU\Junior_II\Web Vibecodiing\OJ\docker-compose.yml`. This file defines the complete infrastructure for an Online Judge (OJ) system. Key services include:
*   **Core Services:** `postgres` (version 16-alpine), `redis` (version 7-alpine), and `minio` (latest) for database, caching/queues, and object storage respectively. All have configured healthchecks and use named volumes for data persistence.
*   **Backend Services:** `api`, `judge-worker`, `judge-monitor`, and `run-worker`.
    *   The `api` service builds from `./backend`, runs Alembic migrations, then starts a Uvicorn server on port 8000.
    *   `judge-worker`, `judge-monitor`, and `run-worker` all build from `./backend` using a `Dockerfile.worker`.
    *   `judge-worker` and `run-worker` are configured with `privileged: true` and mount `/var/run/docker.sock`, indicating they require access to the Docker daemon, likely for creating isolated environments for code execution.
    *   All backend services share common environment variables for connecting to Postgres, Redis, and MinIO, and depend on these core services being healthy. They also reference an `.env` file for configuration.
*   **Frontend/Proxy:** `nginx` builds from `./frontend` and acts as a reverse proxy, exposing port 80 and depending on the `api` service.
*   **Monitoring:** `prometheus` (v2.53.0) and `grafana` (v11.1.0) are included for observability, with Prometheus exposing port 9090 and Grafana port 3000. Grafana is configured with default admin credentials and mounts provisioning and dashboard files.
*   Named volumes are defined for `postgres_data`, `minio_data`, `prometheus_data`, and `grafana_data` to ensure data persistence across container restarts. The backend services consistently use `uv run` for executing Python commands.

The second log entry, dated **6/4/2026, 4:52:23 PM**, shows updates to `c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\ExamManagePage.test.tsx`. This is a Vitest test file for the `ExamManagePage` React component.
*   **Testing Scope:** The tests comprehensively cover the `ExamManagePage`'s functionality, including initial rendering of new and existing exam forms, form interactions, data validation, creation and updating of exams, assignment of problems and candidates, and UI filtering mechanisms.
*   **Mocking:** Extensive use of mocks for external dependencies:
    *   `react-router-dom`'s `useNavigate` is mocked to track navigation.
    *   The `useAuth` hook is mocked to simulate an authenticated admin user.
    *   Various API calls (`apiListProblems`, `apiListAdminUsers`, `apiGetExam`, `apiListAssignments`, `apiCreateExam`, `apiCreateAssignment`, `apiDeleteExam`) are spied upon and mocked to return predefined data, ensuring isolated testing of the component logic.
*   **Test Scenarios:**
    *   Verifies correct rendering of "New Exam" and "Edit:" forms, and the presence of "Create exam", "Delete exam", and "Save changes" buttons.
    *   Tests form validation, asserting that required fields trigger error messages.
    *   Simulates the creation of a new exam, including filling in details (title, description, start/end times, show score), selecting problems and candidates, and verifying that the correct API calls are made and navigation occurs.
    *   (Note: A section describing 'updates existing exam and handles assignment diffs' actually contains logic for deleting an exam, confirming a prompt, calling `apiDeleteExam`, and navigating away).
    *   Includes tests for filtering problems by text and difficulty, and candidates by text (name or email).
    *   Tests "Select all" and "Clear" functionality for both problems and candidates lists, verifying the displayed counts.
*   **Patterns:** The tests consistently use `userEvent` for simulating user interactions, `screen` queries to find elements, `waitFor` for asynchronous UI updates, and `expect(...).toBeInTheDocument()` for assertions, following standard React testing practices.

Both log entries are from the same day and within seconds of each other, suggesting a singular development push or commit that involved both infrastructure setup/refinement for the OJ system and corresponding frontend feature development and testing. The patterns highlight a robust development process involving detailed infrastructure configuration and thorough frontend testing.

## 4:33:47 PM
The provided log details recent updates across an Online Judge (OJ) system, focusing on infrastructure setup and frontend testing.

**File: `c:\NTHU\Junior_II\Web Vibecodiing\OJ\docker-compose.yml` (Timestamp: 6/4/2026, 4:52:10 PM)**
This file defines the entire multi-service application environment using Docker Compose. Key services and their configurations include:
*   **Core Services:** PostgreSQL (version 16-alpine) for the database, Redis (version 7-alpine) for caching, and MinIO (latest) for object storage. All have health checks configured.
*   **Backend Services:**
    *   `api`: Builds from the `./backend` context, uses `.env` (not summarized), and handles API requests. It depends on healthy PostgreSQL, Redis, and MinIO services, running `alembic upgrade head` for database migrations before starting the `uvicorn` server.
    *   `judge-worker`, `judge-monitor`, `run-worker`: These are specialized worker services, all building from `./backend/Dockerfile.worker` and also using `.env`. They depend on healthy PostgreSQL and Redis. The `judge-worker` and `run-worker` services are configured with `privileged: true`, `security_opt: - seccomp:unconfined`, and mount `/var/run/docker.sock`, indicating they are likely responsible for executing code submissions in isolated Docker containers. The `judge-monitor` runs a `worker.py monitor` script.
*   **Frontend Service:** `nginx`: Builds from the `./frontend` context, serves on port 80, and proxies requests, depending on the `api` service. It uses a custom `nginx.conf`.
*   **Observability Services:**
    *   `prometheus` (v2.53.0): Configured for monitoring, exposing port 9090, and mounting a custom `prometheus.yml`. It depends on the `api` service.
    *   `grafana` (11.1.0): Configured for visualization, exposing port 3000, with default admin credentials, and mounting provisioning and dashboard configurations. It depends on `prometheus`.
*   **Volumes:** Named volumes are defined for `postgres_data`, `minio_data`, `prometheus_data`, and `grafana_data` to persist data.
The use of `uv run` across backend services suggests the adoption of the `uv` tool for managing Python environments and running commands.

**File: `c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\ExamManagePage.test.tsx` (Timestamp: 6/4/2026, 4:52:23 PM)**
This is a comprehensive test file for the `ExamManagePage` React component, which likely allows administrators to create, manage, and assign exams within the OJ system.
*   **Setup:** Utilizes `vitest` for testing, `react-router-dom` for navigation (mocked), and `user-event` for simulating user interactions. It extensively mocks API calls (for `examsApi`, `adminApi`, and `problemsApi`) and the `useAuth` hook to simulate an authenticated admin user.
*   **Initial Rendering Tests:** Verifies that the page correctly renders a "New Exam" form or loads an existing exam's data for editing, displaying appropriate buttons like "Create exam" or "Delete exam" and "Save changes."
*   **Form Interactions and Saving Tests:**
    *   Includes validation checks to ensure required fields (title, start/end time) are populated before saving.
    *   Tests the creation of a new exam, including filling in details (title, description, show score option), selecting specific problems and candidates, and asserting that the correct API calls (`apiCreateExam`, `apiCreateAssignment`) are made and navigation to the new exam's manage page occurs.
    *   Contains a test for deleting an existing exam, checking for confirmation and the `apiDeleteExam` call.
*   **Filtering and Bulk Actions Tests:**
    *   Verifies the functionality of filtering problems by text search and difficulty level.
    *   Tests filtering candidates by text search (name or email).
    *   Confirms the "Select all" and "Clear" buttons correctly manage selections for both problems and candidates lists.

**Patterns and Recurring Elements:**
*   **Online Judge (OJ) System:** Both changes are integral to an OJ platform, one setting up the backend/frontend infrastructure and the other testing a core admin feature.
*   **Comprehensive Testing:** The detailed test file for `ExamManagePage` demonstrates a strong emphasis on frontend testing, covering UI rendering, form validation, user interactions, and API integration through mocking.
*   **Containerization:** The `docker-compose.yml` heavily leverages Docker to manage and orchestrate all services, including development, worker, and monitoring components.
*   **API-driven Frontend:** The frontend component (`ExamManagePage`) interacts with the backend via specific API calls (`examsApi`, `problemsApi`, `adminApi`), which are mocked in the tests, highlighting the architecture.
*   **Admin Functionality:** The presence of an `ExamManagePage` test and mocks for `adminApi` and an 'admin' role indicates significant administrative features are being developed and tested.
*   **Timestamp Proximity:** Both file changes occurred on the same day (June 4, 2026) within a short timeframe (seconds apart), suggesting they were part of a single commit or development session.

## 10:44:00 PM
The `docker-compose.yml` file, timestamped 6/4/2026, 4:52:10 PM, defines a multi-service Docker environment for an Online Judge (OJ) system. It sets up core infrastructure including PostgreSQL (version 16-alpine), Redis (version 7-alpine), and MinIO object storage. The application services consist of an `api` backend, and several `worker` services (`judge-worker`, `judge-monitor`, `run-worker`), all built from a backend context and relying on the `postgres`, `redis`, and `minio` services for their healthy operation. A `nginx` service acts as a reverse proxy for the frontend, built from a frontend context. The setup also includes an observability stack with `prometheus` (v2.53.0) and `grafana` (v11.1.0). Common patterns across backend services include: dependency on healthy database and messaging services, and environment variables configured for database, Redis, and MinIO connections. Notably, `judge-worker` and `run-worker` are configured as `privileged` containers with `seccomp:unconfined` and mount `/var/run/docker.sock`, indicating they are designed for secure code execution or Docker-in-Docker scenarios, which is typical for judging environments. All persistent data for `postgres`, `minio`, `prometheus`, and `grafana` is managed via named Docker volumes.

The `frontend\tests\ExamManagePage.test.tsx` file, timestamped 6/4/2026, 4:52:23 PM, contains comprehensive Vitest unit tests for the React `ExamManagePage` component. These tests cover various scenarios:
*   **Initial Rendering:** Verifies the correct display of "New Exam" forms and "Edit" views for existing exams, including the presence of "Create exam," "Save changes," and "Delete exam" buttons.
*   **Form Interactions and Saving:** Validates required fields, simulates the creation of a new exam (filling in details like title, description, times, enabling "show score," selecting problems and candidates), and asserts that the `apiCreateExam` and `apiCreateAssignment` functions are called with the correct data, followed by navigation. It also includes a test for deleting an existing exam.
*   **Filtering and Bulk Actions:** Tests the functionality of filtering problems by text and difficulty, filtering candidates by text, and the "Select all" and "Clear" actions for both problem and candidate lists.
The tests extensively use `vi.spyOn` for mocking API calls from `examsApi`, `adminApi`, and `problemsApi`, as well as mocking the `useAuth` hook to simulate an authenticated admin user. `userEvent` is used to simulate user interactions like typing and clicking, and `waitFor` handles asynchronous updates in the UI.