# Activity Summary for 6/4/2026

## 1:18:42 AM
The CI workflow in `.github/workflows/ci.yml` was configured on `5/30/2026, 9:26:23 PM`. This workflow, named "CI," triggers on pushes and pull requests to the `main` branch, and also manually. It defines two jobs: `backend` for running Python `pytest` tests using `uv`, and `frontend` for running `tsc` (type checking), `vitest` (tests), and `npm run build` using Node.js 20.

The `frontend/src/pages/ProblemEditor.tsx` file saw two updates.
The initial version, timestamped `5/30/2026, 10:30:21 PM`, established a React component for a problem editor. It allows users to select a programming language (Python 3 or C++17), write code in a Monaco Editor, and submit solutions. Key features included loading/saving draft code to `localStorage` and reusing code from previous submissions via `sessionStorage`. Problem details were initially fetched using `apiGetProblem` with a direct problem ID.
A subsequent update to `ProblemEditor.tsx` on `6/1/2026, 1:44:47 AM` changed how problem data is retrieved. Instead of using `apiGetProblem`, it now imports `apiListExamProblems` and fetches problems for a specific `examId`. The component then finds the relevant `problemId` within the list of problems for that exam. This modification ensures that candidates access problems through an exam-scoped endpoint, preventing direct access to a problem bank meant for staff. The problem type was also updated from `Problem` to `ExamProblem`.

Tests for the `CandidateDashboard` component were updated in `frontend/tests/CandidateDashboard.test.tsx` on `5/31/2026, 12:48:13 AM`. These tests verify the dashboard's rendering for candidates, including displaying active, upcoming, and ended exams, problem counts, and last submission status. Crucially, the tests assert that staff-specific management links are absent for candidates, reinforcing role-based access control. Filtering functionality for exams by status and search text is also tested.

A recurring pattern in these changes is a strong focus on the frontend, particularly on user-facing pages and their corresponding tests. There's a consistent emphasis on role-based access, with explicit logic to differentiate between candidate and staff interactions (e.g., how problems are fetched for candidates, and the absence of staff management links on the candidate dashboard). The project utilizes a modern web development stack, including React, Monaco Editor, Vitest, and GitHub Actions for continuous integration. Client-side state management for user drafts and submission reuse is also a repeated element in the `ProblemEditor` component.

## 2:18:49 AM
The log details recent development on an Online Judge (OJ) project, primarily focusing on the frontend, with a notable initial setup for continuous integration.

**File-specific Updates:**

*   **c:\NTHU\Junior_II\Web Vibecodiing\OJ\.github\workflows\ci.yml (Timestamp: 5/30/2026, 9:26:23 PM):**
    This entry details the initial setup of a GitHub Actions CI workflow. It defines two jobs:
    *   **Backend (pytest):** Runs on `ubuntu-latest`, checks out code, sets up `uv` with cache, installs dev dependencies, and executes Python tests.
    *   **Frontend (tsc + vitest + build):** Also runs on `ubuntu-latest`, checks out code, sets up Node.js v20 with npm cache, installs dependencies, performs TypeScript type checking (`--noEmit`), runs `vitest` tests, and builds the frontend application.
    This workflow ensures that code changes are automatically tested and validated for both backend and frontend components upon `push` to `main` or `pull_request` against `main`, and can also be triggered manually.

*   **c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ProblemEditor.tsx (Timestamp: 5/30/2026, 10:30:21 PM):**
    This timestamp marks the introduction or significant update of the `ProblemEditor` page. This React component allows users to view problem statements and submit solutions. Key features include:
    *   Integration with `@monaco-editor/react` for code editing.
    *   Support for multiple languages (Python 3, C++17) with corresponding starter code and Monaco language mappings.
    *   Draft saving and loading to `localStorage` based on exam, problem, and language.
    *   Loading and reusing previously submitted code from `sessionStorage`.
    *   Display of problem details (title, time/memory limits, description, input/output formats, sample cases) through the `ProblemStatement` component.
    *   Real-time submission feedback (status, verdict, score, runtime, memory, errors) via the `SubmissionPanel` and `useSubmissionPoller` hook.
    *   API interaction primarily through `apiGetProblem` to fetch problem details and `apiCreateSubmission` to submit code.

*   **c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\CandidateDashboard.test.tsx (Timestamp: 5/31/2026, 12:48:13 AM):**
    This entry introduces unit tests for the `CandidateDashboard` component using Vitest and `@testing-library/react`. The tests:
    *   Mock API calls for `apiListExams`, `apiListExamProblems`, and `apiListSubmissions` to provide controlled data.
    *   Verify the correct rendering of exam lists for candidates, including details like exam status, problem counts, and last submission verdict.
    *   Confirm that staff-specific management links are correctly omitted for candidates.
    *   Test filtering functionality by exam status and search text, ensuring dynamic updates to the displayed exam list.
    This demonstrates a focus on ensuring the `CandidateDashboard`'s functionality and user experience for candidates are robust and correctly implemented.

*   **c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ProblemEditor.tsx (Timestamp: 6/1/2026, 1:44:47 AM):**
    This is an update to the `ProblemEditor.tsx` file from the previous entry. The primary change is a refactor in how problem data is fetched:
    *   The import for `apiGetProblem` was removed and `apiListExamProblems` from `../api/exams` was added.
    *   The `ProblemStatement` component's `problem` prop type was updated from `Problem` to `ExamProblem`.
    *   Instead of directly calling `apiGetProblem` for a single problem, the `useEffect` hook now calls `apiListExamProblems` (an exam-scoped endpoint) to retrieve all problems for a given exam, and then filters this list to find the specific `problemId`. This change explicitly notes that candidates access problems via exam-scoped endpoints, differentiating it from potential staff-only problem bank access.

**Patterns and Recurring Elements:**

*   **Frontend-Centric Development:** The majority of changes occur within the `frontend` directory, indicating active development on the user interface and client-side logic of the OJ platform.
*   **Sequential Development:** Changes are logged in a clear chronological order from May 30th to June 1st, 2026, showing continuous progress.
*   **Authentication and API Interaction:** Components like `ProblemEditor` and `CandidateDashboard` heavily rely on `useAuth` and `getAccessToken` for authenticated API calls (`apiListExams`, `apiCreateSubmission`, `apiListExamProblems`, etc.), highlighting a secure client-server interaction model.
*   **Problem and Exam Context:** The application design strongly revolves around `examId` and `problemId` parameters, indicating a structure for competitive programming contests or online exams.
*   **Client-Side Data Persistence:** `localStorage` is used for saving code drafts, and `sessionStorage` for reusing submissions, enhancing user experience by preserving progress.
*   **Testing Culture:** The introduction of comprehensive tests for `CandidateDashboard` suggests a commitment to code quality and preventing regressions in critical user-facing features.
*   **Modern Web Stack:** The use of React, TypeScript, Vitest, Monaco Editor, and GitHub Actions (with `uv` for Python) points to a modern development toolchain.

## 4:04:07 AM
The CI workflow (`.github/workflows/ci.yml`) was updated on 5/30/2026, 9:26:23 PM. This workflow defines continuous integration steps for both backend and frontend. The backend job uses `pytest` for testing Python code, utilizing `uv` for dependency management. The frontend job uses `tsc` for type checking, `vitest` for testing, and `npm run build` for building the application, running on Node.js v20. This indicates a robust CI setup for a full-stack project.

The `ProblemEditor.tsx` file received two updates. The first significant update occurred on 5/30/2026, 10:30:21 PM. This file defines the problem editor page, allowing users to view a problem statement, write code in a Monaco editor, select languages (Python 3 and C++17 with starter code), save code drafts locally, and submit solutions. It integrates with backend APIs (`apiGetProblem`, `apiCreateSubmission`) and includes components for displaying problem details (`ProblemStatement`) and submission results (`SubmissionPanel`). The page uses `useSubmissionPoller` to fetch real-time submission status.

On 5/31/2026, 12:48:13 AM, a new test file `CandidateDashboard.test.tsx` was added. This file introduces comprehensive tests for the `CandidateDashboard` component. It includes utilities for mocking API responses and authentication, ensuring the dashboard correctly renders exam lists (active, upcoming, ended), displays submission results, and filters exams by status and search text. Crucially, it asserts that staff-specific management links are not visible to candidate users, highlighting a focus on role-based UI presentation.

The `ProblemEditor.tsx` file was updated again on 6/1/2026, 1:44:47 AM. This update changed how problem data is fetched for candidates. Previously, it used `apiGetProblem` to fetch a single problem. Now, it uses `apiListExamProblems` to retrieve all problems associated with a specific exam, then filters this list to find the target problem. This change reflects a shift towards an exam-scoped approach for candidates accessing problems, enforcing that problems are viewed within the context of an exam rather than from a general problem bank. The type definitions were also updated from `Problem` to `ExamProblem` to reflect this context.

**Key Patterns and Recurring Elements:**
- **Frontend Focus:** All changes are concentrated on the frontend (`frontend` directory), involving React components, state management, API integration, and testing.
- **API Interaction:** Components consistently interact with backend APIs for data retrieval and submission.
- **User Experience Enhancements:** Features like local draft saving and submission reuse are implemented for a better user experience.
- **Role-Based Functionality:** There is a clear distinction in how candidates access information (e.g., problem loading via exam-scoped endpoints, absence of staff-only links on candidate dashboards), indicating a design with different user roles in mind.
- **Automated Testing and CI:** The presence of detailed component tests and a comprehensive CI workflow underscores a commitment to code quality and automated validation.
- **TypeScript Usage:** The widespread use of TypeScript (`.tsx` files, type imports, type checking in CI) indicates a focus on type safety and maintainability.
- **Progressive Development:** The timestamps show a continuous development effort across several hours, implying iterative enhancements to the application's frontend functionality and underlying architecture.

## 10:09:35 AM
The CI workflow at **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\.github\workflows\ci.yml`** was updated on **5/30/2026, 9:26:23 PM**. This workflow now configures continuous integration for both backend and frontend components. The backend uses `uv` to manage Python dependencies and runs `pytest` for testing. The frontend uses Node.js 20, `npm ci` for dependency installation, `tsc --noEmit` for type checking, `npm test` for running tests, and `npm run build` for building the application. This setup ensures automated checks on `push` and `pull_request` events to the `main` branch.

The `ProblemEditor` component located at **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\src\pages\ProblemEditor.tsx`** underwent two significant changes.
The first update on **5/30/2026, 10:30:21 PM** introduced a comprehensive editor page. This page allows users to view problem statements, select programming languages (Python 3, C++17), write code using a Monaco Editor, save drafts locally, submit solutions, and receive immediate feedback on their submissions through a `SubmissionPanel`. Initially, it fetched problem details using a direct `apiGetProblem` API call.

A subsequent update to the same `ProblemEditor` file on **6/1/2026, 1:44:47 AM** refactored the problem loading mechanism. Instead of directly fetching a single problem, it now uses `apiListExamProblems` to retrieve all problems associated with a specific `examId` and then finds the relevant problem from that list. This change, accompanied by a clarifying comment, indicates a shift towards candidates accessing problems within the context of an exam rather than from a general problem bank.

On **5/31/2026, 12:48:13 AM**, a new test file **`c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\CandidateDashboard.test.tsx`** was added. This file contains Vitest tests for the `CandidateDashboard` component. The tests verify that the dashboard correctly renders an exam table for candidates, showing active, upcoming, and ended exams, problem counts, and submission statuses, while specifically ensuring staff-only management links are not visible. It also tests the functionality of filtering exams by status and search text.

**Key patterns and recurring elements** in these changes include a strong focus on frontend development, evident in the updates to `ProblemEditor.tsx` and the addition of tests for `CandidateDashboard.test.tsx`. There is a consistent use of API interactions for fetching and submitting data, coupled with robust error handling and authentication (`useAuth`). The application also utilizes client-side storage (`localStorage`, `sessionStorage`) for user experience features like draft saving and submission reuse. The evolution of problem loading in `ProblemEditor.tsx` suggests a continuous refinement of data access patterns, likely to align with different user roles and contexts (e.g., candidate view within an exam). The setup of a CI workflow highlights an emphasis on maintaining code quality and ensuring automated verification of both backend and frontend components.

## 12:09:31 PM
The project's continuous integration (CI) workflow, located in `.github/workflows/ci.yml` and last updated on **5/30/2026, 9:26:23 PM**, is configured to run on `push` and `pull_request` to the `main` branch, as well as manually via `workflow_dispatch`. It consists of two main jobs:
    *   **Backend (pytest):** Uses `uv` for dependency management and runs Python tests (`pytest`).
    *   **Frontend (tsc + vitest + build):** Uses `npm` for dependency management, performs TypeScript type checking (`tsc --noEmit`), runs JavaScript tests (`npm test`), and builds the frontend (`npm run build`).

On **5/30/2026, 10:30:21 PM**, the `frontend/src/pages/ProblemEditor.tsx` file was initially implemented. This React component provides an interface for candidates to view problem statements, write code using `@monaco-editor/react`, and submit solutions. Key features include:
*   Fetching problem details using `apiGetProblem`.
*   Support for Python 3 and C++17 with starter code and language-specific Monaco editor configurations.
*   Saving code drafts to `localStorage` and reusing previous submissions from `sessionStorage`.
*   Displaying submission status, verdict, score, runtime, and memory usage through a `SubmissionPanel`.
*   Handling problem loading, submission creation (`apiCreateSubmission`), and polling for submission results (`useSubmissionPoller`).

A test file, `frontend/tests/CandidateDashboard.test.tsx`, created on **5/31/2026, 12:48:13 AM**, provides comprehensive unit and integration tests for the `CandidateDashboard` component. These tests mock user authentication for a candidate role and API calls (`apiListExams`, `apiListExamProblems`, `apiListSubmissions`). The tests verify correct rendering of the exam table, ensure that staff management links are not visible to candidates, and confirm that exam filtering by status and search text functions as expected.

A significant update to `frontend/src/pages/ProblemEditor.tsx` occurred on **6/1/2026, 1:44:47 AM**. This change modifies how problem data is retrieved for candidates. Instead of directly calling `apiGetProblem` for a specific problem ID, the component now calls `apiListExamProblems` to fetch all problems associated with a given `examId` and then finds the relevant problem within that list. This indicates a shift towards an exam-scoped problem fetching mechanism for candidates, differentiating it from a potential staff-only problem bank API. The problem type was also updated from `Problem` to `ExamProblem`, reflecting this new data structure.

**Key Patterns and Recurring Elements:**
*   **Active Frontend Development:** The majority of changes focus on the frontend, specifically around candidate-facing features related to exams, problems, and submissions.
*   **Consistent Technology Stack:** The project consistently uses React with TypeScript for the frontend, `uv` for Python dependency management, and `npm` for Node.js/JavaScript.
*   **Robust Testing:** The presence of detailed tests for `CandidateDashboard` highlights a commitment to software quality and ensures core functionalities work as intended.
*   **API Interaction and Separation of Concerns:** Both the problem editor and dashboard components interact with distinct API endpoints for exams and submissions, indicating a well-structured backend API design.
*   **Recent Development:** All timestamps fall within a concentrated period from late May to early June 2026, suggesting active and focused development efforts.

## 1:09:30 PM
The CI workflow in `.github/workflows/ci.yml` was established on **5/30/2026, 9:26:23 PM**. This workflow is configured to run on pushes and pull requests to the `main` branch, as well as manually via `workflow_dispatch`. It includes two main jobs:
*   **Backend (pytest):** Runs on `ubuntu-latest`, checks out code, sets up `uv` (Python package manager) with caching, installs dependencies (`uv sync --frozen --extra dev`), and executes pytest tests.
*   **Frontend (tsc + vitest + build):** Also runs on `ubuntu-latest`, checks out code, sets up Node.js v20 with npm caching, installs dependencies (`npm ci`), performs type checking (`npx tsc --noEmit`), runs tests (`npm test`), and builds the frontend (`npm run build`).

The `frontend\src\pages\ProblemEditor.tsx` file saw two significant updates.
*   The initial version, recorded on **5/30/2026, 10:30:21 PM**, established the `ProblemEditor` component. This component allowed users to view problem statements, edit code in a Monaco Editor, and submit solutions. It handled language options (Python 3, C++17), draft code saving to local storage, and submission polling. Problem details were fetched using `apiGetProblem` and typed as `Problem`.
*   A subsequent update to `frontend\src\pages\ProblemEditor.tsx` on **6/1/2026, 1:44:47 AM** introduced a crucial change in how problems are loaded. Instead of directly fetching a problem by its ID (`apiGetProblem`), the component now uses `apiListExamProblems` to fetch all problems associated with a specific `examId` and then finds the relevant problem within that list. This change is explicitly noted as catering to candidates, who read problems via an exam-scoped endpoint rather than a staff-only problem bank. The problem type was also updated from `Problem` to `ExamProblem` to reflect this context.

Unit tests for the `CandidateDashboard` were added in `frontend\tests\CandidateDashboard.test.tsx` on **5/31/2026, 12:48:13 AM**. These tests use `vitest` and `react-testing-library` to:
*   Mock authentication for a candidate user.
*   Mock API calls for listing exams, exam problems, and submissions.
*   Verify that the dashboard correctly renders exams with their status (active, upcoming, ended), problem counts, and last submission verdict for candidates.
*   Ensure that staff-specific management links are not visible to candidates.
*   Confirm filtering functionality by exam status and search text works as expected.

**Key patterns and recurring elements** across these changes include a focus on distinct user roles (candidates vs. staff), robust frontend testing practices, and a clear separation of concerns in API interactions based on the context of usage (e.g., general problem bank vs. exam-specific problems). The adoption of a comprehensive CI pipeline underscores a commitment to code quality and automated validation for both backend and frontend components.

## 5:07:31 PM
The changes, occurring almost simultaneously on 6/4/2026 at approximately 4:52 PM, reflect significant development in an Online Judge (OJ) application, covering both its infrastructure and a core frontend component's testing.

**File: `c:\NTHU\Junior_II\Web Vibecodiing\OJ\docker-compose.yml` (Timestamp: 6/4/2026, 4:52:10 PM)**
This file defines the entire multi-service infrastructure for the OJ system using Docker Compose. Key services include:
*   **Core Backend Services:** `postgres` (version 16-alpine) for the database, `redis` (version 7-alpine) for caching/messaging, and `minio` (latest) for S3-compatible object storage. All are configured with persistent volumes, environment variables, and health checks.
*   **Application Services:**
    *   `api`: The main backend API, built from a `./backend` context, uses `uvicorn` to run the FastAPI application, and `alembic` for database migrations. It depends on `postgres`, `redis`, and `minio` being healthy.
    *   `judge-worker`, `judge-monitor`, `run-worker`: These are backend worker services, also built from the `./backend` context using a `Dockerfile.worker`.
        *   `judge-worker` and `run-worker` are notably configured with `privileged: true` and mount `/var/run/docker.sock`, indicating they are designed to interact directly with the Docker daemon, likely for sandboxing or managing code execution environments for judging user submissions.
        *   `judge-monitor` is specifically tasked with running `worker.py monitor`.
        *   `run-worker` is configured to run an `rq worker` with a scheduler.
    *   All backend application services (`api`, `judge-worker`, `judge-monitor`, `run-worker`) share common environment variables for connecting to `postgres`, `redis`, and `minio`, indicating a unified backend configuration.
*   **Frontend Service:** `nginx` is set up as a reverse proxy for the frontend, built from `./frontend`, exposing port 80, and serving a custom `nginx.conf`. It depends on the `api` service.
*   **Observability:** The setup integrates `prometheus` (v2.53.0) for monitoring and `grafana` (v11.1.0) for visualization, with custom configurations and data persistence volumes. Grafana is configured with default admin credentials.
*   **Data Persistence:** Dedicated volumes are defined for `postgres`, `minio`, `prometheus`, and `grafana` data.

**File: `c:\NTHU\Junior_II\Web Vibecodiing\OJ\frontend\tests\ExamManagePage.test.tsx` (Timestamp: 6/4/2026, 4:52:23 PM)**
This file provides comprehensive frontend tests for the `ExamManagePage` React component, focusing on its functionality for creating, editing, and managing exams.
*   **Testing Setup:** It uses `vitest` for the test runner, `@testing-library/react` for UI interactions, and `react-router-dom`'s `MemoryRouter` for isolated routing tests. Key dependencies like `useAuth` and `useNavigate` are mocked to control authentication state and navigation flow within tests.
*   **API Mocking:** Extensive mocking of API calls (`examsApi`, `adminApi`, `problemsApi`) is in place to ensure tests are isolated from the backend and focus on the component's behavior. Sample data for problems and candidates is provided.
*   **Test Suites:**
    *   **Initial Rendering:** Verifies that the page correctly renders forms for new exams and loads existing exam data for editing, including the presence of action buttons like "Create exam", "Save changes", and "Delete exam".
    *   **Form Interactions and Saving:**
        *   Tests validation logic, ensuring required fields trigger error messages.
        *   Simulates the creation of a new exam, including filling form fields (title, description, start/end times), toggling `show_score`, and selecting problems and candidates. It asserts that the correct API calls (`apiCreateExam`, `apiCreateAssignment`) are made and that navigation occurs as expected.
        *   Includes a test for deleting an existing exam, verifying confirmation dialogs and API calls.
    *   **Filtering and Bulk Actions:**
        *   Tests the ability to filter problems by text search and difficulty.
        *   Tests filtering candidates by text search.
        *   Verifies the "Select all" and "Clear" functionalities for both problem and candidate lists.

**Patterns and Recurring Elements:**
*   **Project Focus:** Both logs relate to an "Online Judge" system, with `docker-compose.yml` setting up the backend infrastructure (judge-workers, API) and `ExamManagePage.test.tsx` testing the exam management feature of the frontend.
*   **Modern Web Stack:** The project utilizes a modern stack including Docker/Docker Compose for deployment, PostgreSQL, Redis, MinIO, a Python/FastAPI backend, and a React frontend.
*   **API-driven Frontend:** The frontend tests heavily rely on mocking API calls, highlighting a clear separation between frontend and backend, with the frontend consuming the API provided by the `api` service.
*   **Robustness:** The infrastructure definitions are comprehensive, including health checks and observability, while the frontend tests demonstrate a strong commitment to component reliability and user experience.
*   **Security/Isolation for Judging:** The privileged worker containers mounting `/var/run/docker.sock` indicate an advanced strategy for sandboxed code execution, which is crucial for an OJ system.